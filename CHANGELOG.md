# Changelog

## 0.4.1 — 2026-09-21

Mechanical chrome FAIL gate + recipes so `/hig` is model-proof.

- `scripts/check-chrome.mjs` scans host UI source; P0 hits block `HIG_CHROME` PASS
- Recipes: `knowledge/chrome/recipes.md`
- New FAIL IDs: `chrome.materials.fashion-glass`, `chrome.layout.card-grid-home`, `chrome.ive.nested-cards`
- Round 1 apply = 12 required surfaces from `surfaces.yaml`; optional packs wait until core P0 is clean
- Product + unspecified fonts stay with the host (no hardcoded SF Pro)
- `chrome.list-browser.filter-density` is a required grammar class
- Eval: `eval/run-check-chrome.mjs` plus pass/fail fixtures
- Later catalog goal-loop plan: `docs/plans/2026-09-21-hig-catalog-goal-agent.md` (additive; does not replace `requiredIds`)
- Catalog inventory: `knowledge/catalog.yaml` from Apple's live index (`scripts/sync-hig-catalog.mjs`). Round 1 apply still 12 `requiredIds`. Eval: `eval/run-catalog.mjs`
- Catalog goal loop after chrome P0: `scripts/plan-catalog.mjs` writes `.hig/catalog-status.yaml`. Wave 0 stays `requiredIds`. Then apply remaining packed applicable topics until `remaining` is 0. Brand app-shell rows are `n/a-register`. Watch/TV/Vision skip on web. Pattern `affordance` skips when the host has no matching widget. Same chrome P0 FAILs proven on a web host and a Swift host without naming those stacks in the rule.
- Catalog `failWhen` / `passWhen` derived at load from `grammar.yaml` + pack Do/Don't (not title stubs, not Apply-in-host). Unpackaged rows stay stubs.
- Mechanical apply for dual-stack P0s: `scripts/apply-chrome.mjs` (`chrome.view-mode.icons`, `chrome.bars.system-materials`) on a web host and a Swift host; no kit or font rewrite. Other P0s still use `recipes.md`.
- Mechanical apply for remaining P0 IDs (`toolbar-budget`, `form.column-cohesion`, `fashion-glass`, `card-grid-home`) plus `nested-cards` on tmp copies of `chrome-antipatterns`. Filter-density and sidebar still use `recipes.md`. Committed fixtures stay dirty.
- Mechanical apply for remaining P1 IDs (`filter-density`, `sidebar.collapsible`). `apply-chrome.mjs` now owns every grammar ID. Committed fixtures stay dirty.
- Catalog apply: `scripts/apply-catalog.mjs` accounts packed topics whose `chromeIds` are clean after chrome P0. Pack-only pending topics stay pending. Eval on tmp `chrome-pass` and `chrome-antipatterns`.
- Catalog apply joins pack **Chrome gates** into topic `chromeIds` and accounts pack **Don't** backtick tokens (strip `scaleX(-1)`, map physical margin/padding, no kit). Prose-only Don'ts stay pending. Remaining is not a frozen integer.
- Catalog apply mechanically scans required-surface prose Don'ts (type, color, motion, a11y) and accounts a topic only when every Don't has a scanner. Host fonts stay. Optional packed Don'ts stay pending. Remaining is not a frozen integer.
- Optional widget affordances (`menu`, `picker`, `progress`, `search`, `notification`, `loading`, `feedback`, `onboarding`, `drag`) skip with `skipped-no-affordance` when the host has no matching widget. Search field ≠ Search nav link. `<select>` is a picker, not a menu. Presence without a Don't scanner still stays pending. `requiredIds` stay 12.
- Search Don't heuristics: hide-only-path, spinner-per-keystroke, search-as-settings-dump. chrome-pass search is already-compliant. Unfixable hide-only-path and command-dump stay pending. Do not invent a list. `requiredIds` stay 12.
- Settings and undo skip-unless affordances: hosts without a settings/preferences screen or undo chrome are `skipped-no-affordance`. A form is not Settings. Cancel is not Undo. Presence without a Don't scanner stays pending. Other foundations stay pending. `requiredIds` stay 12.
- Writing Don't heuristics: sarcastic errors that hide the fix, Title Case on long body help, rewritten Sign in/Pay/permission alerts. chrome-pass writing is already-compliant. Unfixable sarcasm and system-alert rewrites stay pending. Title Case help sentence-cases. Do not invent the next step or rewrite brand voice. Other foundations stay pending. `requiredIds` stay 12.
- Privacy Don't heuristics: dark-pattern Allow-only, pre-emptive camera/mic/location on marketing, rewritten or automated system permission/Pay/Sign in. chrome-pass privacy is already-compliant. Hidden Don't Allow unhides. Allow-only and marketing prompts stay pending. Do not invent a decline control or permission copy. Other foundations stay pending. `requiredIds` stay 12.
- Branding Don't heuristics: opaque brand fills on nav/tool/tab bars, watermarks on content, rewriting SF Symbols into a custom outlined set. chrome-pass branding is already-compliant. Watermarks strip. Mixed outlined/system toolbars stay pending. Do not inject a brand kit or rewrite host fonts. Other foundations stay pending. `requiredIds` stay 12.
- Icons / images / app-icons / inclusion Don't heuristics: SF Symbol name tables, outlined doodles next to system symbols, decorative heading icons, screenshot empty-states, bitmap SF Symbols, copied scale-factor tables, Icon Composer templates as law, busy photo app icons, app-icon alpha/mask tricks, diversity stock, ability/body jokes, locked skin-tone defaults. chrome-pass icons/images/app-icons/inclusion are already-compliant. Decorative heading icons, screenshot dumps, and app-icon masks strip. Unfixable tables/bitmaps/templates/jokes stay pending. Design-principles Don'ts stay pending. Do not inject an icon kit or invent copy. `requiredIds` stay 12.
- Optional-widget Don't heuristics: settings, undo, loading, feedback, onboarding, drag, notifications, launching. chrome-pass still skips missing widgets. Clean Settings/Undo hosts are already-compliant. Settings re-theme fills, CRUD confetti, and launch autoplay strip. Unfixable first-run walls, delete confirms, and notification walls stay pending. Do not invent a missing widget. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Chrome-backed Don't heuristics: layout, materials, lists, forms, navigation. chrome-pass those topics are already-compliant. List min-widths, equal-weight submits, list-toolbar CTAs, marketing tab shells, stacked glass, compatibility flags, relative bottom nav, and card-grid masters strip/move. Packs with Don't bullets account through Don't scanners, not clean chromeIds. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Host-widget Don't heuristics: menus, pickers, progress, controls. chrome-pass still skips missing menus/pickers/progress; buttons stay already-compliant. Hidden menu items unhide+dim; overweight wheels become native `<select>`; progress morph/jump/pull-to-refresh titles strip; `OK`+save and submit-toggles rewrite. Nested submenus, mixed menu icons, picker-owned screens, bare steppers, and `OK` without a verb stay pending. Do not invent a field, route, or icon kit. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- System-chrome Don't heuristics: widgets, Live Activities, status bars, Control Center. chrome-pass still skips those gates. Clean capability hosts are already-compliant. Fake in-app widgets, stretched small widgets, Dynamic Island pointers, Live Activity ads, hidden status bars, fake status clocks, opaque status strips, and settings-row Control Center chrome strip. App-icon-as-widget and one-symbol Control Center toggles stay pending. Do not invent a widget kit. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- RTL Don't heuristics: whole-window `scaleX(-1)`, physical `margin-left` / `padding-right`, back chevrons that always point left, `dir="auto"` on `html` or the locale root. chrome-pass right-to-left is already-compliant. Whole-window mirrors, physical chrome margins, `dir="auto"` roots, and `chevron.left` / `chevron-left` back controls rewrite to logical. Unicode `←` back chevrons stay pending. Do not invent a mirrored design file. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Nested-modal Don't heuristics: sheets, alerts, action-sheets, modality. chrome-pass still skips missing overlays. A `data-nested-modal` marker strips. Nested `<dialog>` stacks stay pending without flattening into a missing surface. Do not invent a sheet. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Composed `also:` catalog wiring: dark-mode rides color Don'ts, SF Symbols rides icons Don'ts, context/pull-down/pop-up buttons ride menu Don'ts, notifications glanceability rides managing-notifications Don'ts. No second swarm lease. The Mac menu bar and tab-views stay unpackaged stubs. `requiredIds` stay 12.
- Slider, scroll-view, and popover Don't heuristics from Apple guidance: do not use a slider for audio volume; do not nest same-orientation scroll views; do not cascade popovers, use a popover as a warning, or keep a popover in compact width. chrome-pass still skips missing sliders/scroll panes/popovers. Markers strip. A live volume slider, nested overflow panes, and nested popovers stay pending without inventing a volume view, flattening scroll, or turning a popover into a sheet. `type=range` is not a picker. Document/body overflow is not a scroll view. A sheet `dialog` is not a popover. Do not invent a missing widget. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Collection Don't heuristics from Apple guidance: do not use a custom layout that confuses; do not put a collection of text where a table belongs; do not let collection items overlap. chrome-pass still skips missing collections. A `<ul>` inventory stays a list. Markers strip on a visual `data-collection` with an image. A text-only collection of spans stays pending without inventing a table or flattening into a list. Do not map collections onto lists. Do not invent a missing widget. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Page-control Don't heuristics from Apple guidance: do not use a page control for hierarchical or nonsequential pages; do not show more than about 10 dots; do not use more than two different indicator images; do not color indicator images. chrome-pass still skips missing page controls. A progress bar and numbered pagination stay themselves. Markers strip. A live `numberOfPages={12}` host stays pending without inventing a grid. Do not invent a missing widget. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Label Don't heuristics from Apple guidance: do not use a label for text people need to edit; do not use a label for a large amount of text; do not make useful label text uncopyable. chrome-pass still skips missing static labels. A form `<label>` and `aria-label` stay themselves. Markers strip. A live `contenteditable` label stays pending without inventing a text field. Host fonts stay. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Text-view Don't heuristics from Apple guidance: do not use a text view for a small amount of text; do not make useful text-view text uncopyable. chrome-pass still skips missing text views. An `<input>` stays a field. Markers strip. A live 1-row textarea stays pending without inventing a label. Host fonts stay. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Image-view Don't heuristics from Apple guidance: do not add button behaviors to an image view; do not use an image view for an interface icon; do not overlay text on an image view. chrome-pass still skips missing image views. Every `<img>` stays itself. Markers strip. A live clickable `data-image-view` stays pending without inventing a button. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Chart Don't heuristics from Apple Charts and Charting data: do not rely solely on color; do not hide critical information behind interaction; do not chart data that belongs in a table or list; do not pack a chart with too much data. chrome-pass still skips missing charts. A table and a decorative SVG stay themselves. `charting-data` rides the same pack via `also:`. Markers strip. A live hover-only `data-chart` stays pending without inventing a table. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Disclosure-control Don't heuristics from Apple guidance: do not use more than one disclosure button in a view; do not use a disclosure triangle without a descriptive label; do not show advanced details without hiding them until they're relevant. chrome-pass still skips missing disclosure widgets. An `aria-expanded` toolbar stays itself. Markers strip. A live unlabeled `<details>` stays pending without inventing a summary. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Box Don't heuristics from Apple Boxes: do not nest boxes to define subgroups; do not size a box close to its containing view. Distinct from `chrome.ive.nested-cards` (list/form extra card panels). chrome-pass still skips missing box widgets. A card, grouped list, and form fieldset stay themselves. Markers strip. Live nested `data-box` stays pending without un-nesting. Do not invent a missing box. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Edit-menu Don't heuristics from Apple guidance: do not invent a custom menu that presents the same commands; do not show Cut or Copy when nothing is selected; do not add other controls that perform the same functions. chrome-pass still skips missing edit menus. A command `role="menu"` stays a menu. Markers strip. Live Cut/Copy on `data-edit-menu` without a selection stays pending without inventing dimming. Do not invent Cut, Copy, or Paste. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Offering-help Don't heuristics from Apple guidance: do not tell people to click on iPhone or tap on Mac; do not explain how standard components work; do not put promotional content in a tip. chrome-pass still skips missing tips. Onboarding and a `title=` attribute stay themselves. Markers strip. Live "click on iPhone" help copy stays pending without inventing platform verbs. Do not invent help. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Web-view Don't heuristics from Apple guidance: do not omit forward and back on a multi-page web view; do not replicate Safari. chrome-pass still skips missing web views. The host document stays itself. Markers strip. A live multi-page `iframe` without Back/Forward stays pending without inventing chrome. Do not invent a web view. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Activity-view Don't heuristics from Apple guidance: do not duplicate common share-sheet actions; do not reveal the sheet with an alternative to Share. chrome-pass still skips missing share sheets. A Share nav link stays itself. Markers strip. Live duplicate Print rows stay pending without inventing a custom title. Do not invent a share sheet. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Printing Don't heuristics from Apple guidance: do not show Print when nothing is printable; do not duplicate system page-orientation options. chrome-pass still skips missing Print actions. A share-sheet Print row stays itself. Markers strip. Live Print on `data-nothing-printable` stays pending without inventing dimming. Do not invent a printer. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Going-full-screen Don't heuristics from Apple guidance: do not programmatically resize the window; do not end full-screen automatically; do not offer a custom menu of window modes. chrome-pass still skips missing full-screen experiences. `100vh` and a video's native controls stay themselves. Markers strip. Live `resizeTo` on `data-fullscreen` stays pending without inventing layout. Do not invent a window. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- File-management Don't heuristics from Apple guidance: do not add a custom top toolbar on a file-browser modal; do not show file extensions by default; do not require an explicit Save to keep work. chrome-pass still skips missing document browsers. `<input type="file">` stays itself. Markers strip. Live Save on `data-no-autosave` stays pending without inventing autosave. Do not invent a file browser. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Focus-and-selection Don't heuristics from Apple guidance: do not change focus without people's interaction; do not replace system focus effects with custom ones. chrome-pass still skips missing focus systems. `:focus` CSS, `tabindex`, and `autofocus` alone stay themselves. Markers strip. Live `.focus()` on `data-focus-system` stays pending without inventing a ring. Do not invent a focus system. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Managing-accounts Don't heuristics from Apple guidance: do not require an account before people can use the app; do not bury account deletion in Privacy Policy or Terms; do not use passcode for account authentication. chrome-pass still skips missing account screens. A form, Settings, a password field, and Sign in with Apple stay themselves. Markers strip. Live `data-no-guest` plus passcode copy on `data-account` stays pending without inventing a guest path, deletion flow, or Face ID. Face ID and Sign in with Apple consent stay human-only. Do not invent an account wall. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Tab-view Don't heuristics from Apple guidance: do not use a pop-up to switch tabs; do not provide more than six tabs; do not let pane controls affect another pane. chrome-pass still skips missing tabbed panes. A bottom tab bar and a tablist without a tabpanel stay themselves. Markers strip. Live seven `role="tab"` items stay pending without inventing a menu. Do not invent a tab strip. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Multitasking Don't heuristics from Apple guidance: do not keep attention-requiring activity going when people switch away; do not notify when a routine or secondary task completes; do not ignore a primary audio interruption. chrome-pass still skips missing multitasking sessions. A video element stays itself. Markers strip. Live `data-keep-playing` on `data-multitask` stays pending without inventing `visibilitychange`, `.pause()`, or a notification. Do not invent pause/resume chrome. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Ratings-and-reviews Don't heuristics from Apple guidance: do not ask on first launch or during onboarding; do not interrupt a task; do not pester with repeated requests. chrome-pass still skips missing ratings prompts. A star glyph and onboarding stay themselves. Markers strip. Live double `requestReview()` on `data-rating-prompt` stays pending without inventing StoreKit or a delay. Do not invent a review sheet. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Windows Don't heuristics from Apple guidance: do not open new windows as default; do not create custom window frames or controls; do not call a window a scene; do not put critical actions in a bottom bar. chrome-pass still skips missing app windows. The host document and `UIWindow` stay themselves. Markers strip. Live `data-window` footer Save stays pending without inventing an inspector. Do not invent a window. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Playing-video Don't heuristics from Apple guidance: do not ship a custom player that diverges from the system player; do not embed letterbox or pillarbox padding; do not ask people if they want to resume playback; do not put a loading or splash screen before play. chrome-pass still skips missing video players. `100vh` and Picture in Picture chrome stay themselves. Markers strip. Live `data-video-player` Resume playback? stays pending without inventing autoplay or a Space handler. Mixed-audio-on-mode-switch stays on packed multitasking. Do not invent a player. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Playing-haptics Don't heuristics from Apple guidance: do not use a system haptic pattern to mean something else; do not overuse haptics; do not ship haptics with no way to turn them off. chrome-pass still skips missing haptic APIs. A button tap stays itself. Markers strip. Live `navigator.vibrate` on `data-haptic` stays pending without inventing a mute toggle. Do not invent Core Haptics. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- AirPlay Don't heuristics from Apple guidance: do not stop playback when the app backgrounds or the device locks; do not interrupt another app's playback with non-immersive content; do not mirror automatically; do not stream background loops or in-app-only clips. chrome-pass still skips missing AirPlay routing. Every `<video>` and a Share control stay themselves. Markers strip. Live `data-airplay` plus autoplay video stays pending without inventing a route picker. Prefer-system-player stays on packed playing-video. Do not invent AirPlay. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Gyro-and-accelerometer Don't heuristics from Apple guidance: do not gather motion data with no tangible benefit; do not use an accelerometer or gyroscope to directly manipulate the interface outside of active gameplay. chrome-pass still skips missing motion APIs. A swipe and CSS motion stay themselves. Markers strip. Live `data-gyro` plus `devicemotion` stays pending without inventing Core Motion. Do not invent a gyroscope. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Home Screen quick-action Don't heuristics from Apple guidance: do not put the app name or extra copy in a title; do not use an emoji in place of a symbol. chrome-pass still skips missing quick actions. A context menu and Share stay themselves. Markers strip. Live `data-quick-action` plus an emoji title stays pending without inventing an SF Symbol. Do not invent a Home Screen menu. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Live-viewing Don't heuristics from Apple guidance: do not leave live content unmarked versus video-on-demand; do not keep audio playing after people leave the live tab. chrome-pass still skips missing live-viewing chrome. Every `<video>` stays itself. Markers strip. Live `data-live-viewing` plus On demand copy stays pending without inventing a live badge. Packed playing-video and packed multitasking stay themselves. Do not invent live-viewing chrome. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Snippet Don't heuristics from Apple guidance: do not rely on spoken dialogue text to convey a snippet's purpose; do not ship a snippet custom view taller than 400 points. chrome-pass still skips missing snippets. A card and packed App Shortcuts stay themselves. Markers strip. Live `data-snippet` plus height 480 stays pending without inventing a max-height. Do not invent a snippet. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Generative-AI Don't heuristics from Apple guidance: do not present AI content as human-authored; do not ship generated content with no way to dismiss, revert, or retry. chrome-pass still skips missing generative features. A textarea and packed inclusion stay themselves. Markers strip. Live `data-generative` plus a human-authored claim stays pending without inventing disclosure copy, Undo, or Retry. Do not invent a generative feature. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Always-On Don't heuristics from Apple guidance: do not leave sensitive information visible; do not stop motion instantly when Always On begins. chrome-pass still skips missing Always On chrome. Dimmed CSS and packed Live Activities stay themselves. Markers strip. Live `data-always-on` plus a bank balance stays pending without inventing redaction. Do not invent Always On. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- SharePlay Don't heuristics from Apple guidance: do not pair SharePlay with an adjective; do not inflect the term as SharePlayed, SharePlays, or SharePlaying. chrome-pass still skips missing SharePlay chrome. A Share control and packed activity-views stay themselves. Markers strip. Live `data-shareplay` plus Spatial SharePlay stays pending without inventing GroupActivity. Do not invent SharePlay. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Nearby-interaction Don't heuristics from Apple guidance: do not use a nearby interaction as the only way to perform a task; do not explicitly tell people to hold the device in portrait. chrome-pass still skips missing Nearby Interaction chrome. A Share control, NFC, and geolocation stay themselves. Markers strip. Live `data-nearby` plus "the only way to transfer" stays pending without inventing NISession. Do not invent Nearby Interaction. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Activity-rings Don't heuristics from Apple guidance: do not use rings for other types of data; do not use rings for more than one person; do not recolor rings; do not use rings for decoration or branding. Packed `gate: always` overlays the watch slug. chrome-pass still skips missing Activity ring widgets. A progress bar and a CSS circle stay themselves. Markers strip. Live `data-activity-rings` plus other types of data stays pending without inventing HKActivityRingView. Do not invent Activity rings. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- NFC Don't heuristics from Apple guidance: do not ask people to tap or touch to scan; do not say NFC, Core NFC, Near-field communication, or NFC tag in user-facing copy. Packed `gate: always` overlays catalog `capability:nfc`. chrome-pass still skips missing NFC chrome. Nearby Interaction, Wallet, Tap to Pay, and packed Apple Pay stay themselves. Markers strip. Live `data-nfc` plus tap-your-phone / NFC tag copy stays pending without inventing NFCNDEFReaderSession. Do not invent NFC or complete an NFC read. The commerce cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Augmented-reality Don't heuristics from Apple guidance: do not say world detection, Adjust tracking, or plane to anchor; do not alter the AR glyph or badge or use them for a non-ARKit experience. Packed `gate: always` overlays catalog `capability:arkit`. chrome-pass still skips missing AR chrome. Every image, CSS 3D, Nearby Interaction, and NFC stay themselves. Markers strip. Live `data-ar` plus Adjust tracking stays pending without inventing ARView. Do not invent ARKit, a reset, or a coaching view. The media-intelligence cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Tap to Pay Don't heuristics from Apple guidance: do not put the Apple logo in a Tap to Pay button; do not say Tap to Pay or Tap to Pay on iPhone on a Look Up, Store Card, Verify, Refund, or loyalty button. Packed `gate: always` overlays catalog `capability:taptopay`. chrome-pass still skips missing Tap to Pay chrome. NFC, Wallet, and packed Apple Pay stay themselves. Markers strip. Live `data-tap-to-pay` plus Look Up / Tap to Pay on iPhone stays pending without inventing ProximityReader. Do not invent Tap to Pay or complete a payment. The commerce cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- ID Verifier Don't heuristics from Apple guidance: do not put the Apple logo in an ID Verifier button; do not put an NFC or QR communication symbol on a Verify Age or Verify Identity button. Packed `gate: always` overlays catalog `capability:idverifier`. chrome-pass still skips missing ID Verifier chrome. NFC, Tap to Pay, Wallet, and packed Apple Pay stay themselves. Markers strip. Live `data-id-verifier` plus NFC / Verify Age stays pending without inventing MobileDriversLicenseDisplayRequest. Do not invent ID Verifier or complete a verification. The commerce cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- In-app purchase Don't heuristics from Apple guidance: do not modify or replicate the system confirmation sheet; do not hide the refund-request button behind a scroll or extra screen; do not characterize Apple refund policies. Packed `gate: always` overlays catalog `capability:iap`. chrome-pass still skips missing IAP chrome. Packed Apple Pay, Tap to Pay, Wallet, and ID Verifier stay themselves. Markers strip. Live `data-in-app-purchase` plus refund-policy copy stays pending without inventing StoreKit. Do not invent IAP or complete a purchase. The commerce cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Maps Don't heuristics from Apple guidance: do not cover the Maps legal link all the time; do not replicate Apple Maps on an indoor map. Packed `gate: always` overlays catalog `capability:mapkit`. chrome-pass still skips missing MapKit chrome. Packed CarPlay, a generic map word, and an iframe stay themselves. Markers strip. Live `data-map` plus covered-legal-link copy stays pending without inventing MKMapView. Do not invent a map. The carplay-maps cluster stays a live-link cluster (`covers: [carplay]`). Design-principles Don'ts stay pending. `requiredIds` stay 12.
- HomeKit Don't heuristics from Apple guidance: do not suggest company names or model numbers as Siri service names; do not overwrite the HomeKit database without direction; do not duplicate home settings; do not block camera images. Packed `gate: always` overlays catalog `capability:homekit`. chrome-pass still skips missing HomeKit chrome. Packed iCloud, a generic Home app word, and packed managing-accounts stay themselves. Markers strip. Live `data-homekit` plus suggested Siri service-name copy stays pending without inventing HMHomeManager. Do not invent HomeKit. Do not pack require-account. The icloud-shareplay cluster stays a live-link cluster (`covers: [icloud, shareplay]`). Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Workouts Don't heuristics from Apple guidance: do not distract people from an active workout; do not record extremely brief sessions as workouts. Packed `gate: always` overlays catalog `always`. chrome-pass still skips missing workout chrome. Packed Activity rings, packed HealthKit, and a progress bar stay themselves. Markers strip. Live `data-workout` plus list-of-workouts-during-active-session copy stays pending without inventing HKWorkoutSession. Do not invent a workout session. Do not pack the Activity-rings bullet. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Live Photos Don't heuristics from Apple guidance: do not present Live Photo frames or audio separately; do not put a video playback button on a Live Photo; do not replicate the Live Photos experience in an unsupported environment. Packed `gate: always` overlays the cluster appliesWhen. chrome-pass still skips missing Live Photo chrome. Every still photo, every `<video>`, packed playing-video, and a bare Live Photo phrase stay themselves. Markers strip. Live `data-live-photo` plus a video playback button stays pending without inventing PHLivePhotoView. Do not invent a Live Photo. The media-intelligence cluster stays a live-link cluster; dedicated live-photos attaches by appleUrl. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- iCloud Don't heuristics from Apple guidance: do not ask which documents to keep in iCloud; do not alert when iCloud is unavailable; do not store app resources in iCloud. Packed `gate: always` overlays catalog `capability:icloud`. chrome-pass still skips missing iCloud chrome. Packed file-management, packed SharePlay, and a bare iCloud phrase stay themselves. Markers strip. Live `data-icloud` plus which-documents-to-keep copy stays pending without inventing CKContainer. Do not invent iCloud. The icloud-shareplay cluster stays a live-link cluster; dedicated icloud attaches by appleUrl. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Siri Don't heuristics from Apple guidance: do not advertise or pitch IAP in Siri-delivered content; do not impersonate Siri or use reserved phrases; do not refer to Siri with she, he, or her. Packed `gate: always` overlays catalog `capability:siri`. chrome-pass still skips missing Siri chrome. Packed App Shortcuts, packed snippets, packed inclusion, and a bare Siri / Hey Siri phrase stay themselves. Markers strip. Live `data-siri` plus pronoun copy stays pending without inventing INInteraction. Do not invent Siri. Do not complete Siri permission. The siri-app-shortcuts cluster keeps app-shortcuts via `also:`; dedicated siri attaches by appleUrl. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- App Shortcuts leftover Don't heuristics from Apple guidance: do not re-skin Siri or Shortcuts editor chrome; do not write App Shortcuts or Shortcuts in lowercase; do not title-case individual shortcuts. Packed `gate: always` overlays catalog `capability:siri` on the existing cluster (no second exclusive `app-shortcuts` surface). chrome-pass still skips missing App Shortcuts chrome. Packed Siri, packed snippets, and a bare App Shortcuts phrase stay themselves. Markers strip. Live `data-app-shortcuts` plus lowercase copy stays pending without inventing AppShortcutsProvider. Do not invent App Intents. Dedicated siri stays itself. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- HealthKit leftover Don't heuristics from Apple guidance: do not replicate the Health permission screen; do not manage health data sharing in-app; do not say HealthKit to people. Packed `gate: always` overlays catalog `capability:healthkit`. chrome-pass still skips missing HealthKit chrome. Packed HomeKit, packed Activity rings, packed workouts, packed privacy, and a bare HealthKit phrase stay themselves. Markers strip. Live `data-healthkit` plus HealthKit copy stays pending without inventing HKHealthStore. Do not invent HealthKit. Do not complete Health permission. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- CarPlay leftover Don't heuristics from Apple guidance: do not lock people out of CarPlay because iPhone requires input; do not report errors on iPhone instead of CarPlay; do not require iPhone interactions while CarPlay is active. Packed `gate: always` overlays catalog `capability:carplay,capability:mapkit`. chrome-pass still skips missing CarPlay chrome. Packed maps and a bare CarPlay phrase stay themselves. Markers strip. Live `data-carplay` plus unlock-iPhone copy stays pending without inventing CPInterfaceController. Do not invent CarPlay. Do not complete vehicle pairing. Dedicated carplay attaches by appleUrl; the carplay-maps cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Apple Pay leftover Don't heuristics from Apple guidance: do not use the Apple Pay mark as a payment button; do not make Apple Pay plural or possessive; do not use the Apple logo in place of the word Apple. Gate stays `capability:applepay` (not overlaid to always) so a PassKit wallet host does not launch apple-pay. `data-apple-pay` is an Apple Pay signal. chrome-pass still skips the capability gate. Packed Wallet, packed Tap to Pay, packed NFC, packed In-App Purchase, and a bare Apple Pay phrase stay themselves. Markers strip. Live `data-apple-pay` plus Apple Pays copy stays pending without inventing PKPaymentButton. Do not invent Apple Pay. Do not complete the payment sheet. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Sign in with Apple leftover Don't heuristics from Apple guidance: do not ask for a password alongside Sign in with Apple; do not ask for a personal email when a private relay address is used; do not draw a custom Apple logo. Packed `gate: always` overlays catalog `capability:signinwithapple`. chrome-pass still skips missing Sign in with Apple chrome. Packed managing-accounts, packed Apple Pay, and a bare Sign in with Apple phrase stay themselves. Markers strip. Live `data-siwa` plus create-a-password copy stays pending without inventing SignInWithAppleButton. Do not invent Sign in with Apple. Do not complete the system sign-in sheet. Design-principles Don'ts stay pending. `requiredIds` stay 12.

- Playing audio Don't heuristics from Apple guidance: do not set the system output volume; do not repurpose audio controls for a non-playback action; do not continue playback after headphones disconnect. Gate is `always`. chrome-pass still skips missing playing-audio chrome. A volume slider, a video element, packed AirPlay, and a bare audio phrase stay themselves. Markers strip. Live `data-playing-audio` plus system-output-volume copy stays pending without inventing AVAudioSession. Do not invent a player. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Game Center leftover Don't heuristics from Apple guidance: do not show the access point during active gameplay; do not resize or restyle official Game Center artwork; do not write GameKit, game center, Awards, or Rankings in custom links. Gate stays `capability:gamecenter` (not overlaid to always). `data-game-center` is a Game Center signal. chrome-pass still skips the capability gate. A bare Game Center phrase and import GameKit stay themselves. Markers strip. Live `data-game-center` plus access-point-during-gameplay copy stays pending without inventing GKAccessPoint. Do not invent Game Center. Do not complete sign-in. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Panels Don't heuristics from Apple guidance: do not list a panel in the Window menu documents list; do not offer a minimize button on a panel; do not let a HUD obscure the content it adjusts. Gate is `always` and overlays catalog `desktop`. chrome-pass still skips missing panel chrome. A sheet, a regular window, `data-window`, and `NSOpenPanel` stay themselves. Markers strip. Live `data-panel` plus Window-menu-documents-list copy stays pending without inventing NSPanel. Do not invent a panel. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Path controls Don't heuristics from Apple guidance: do not place a path control in a toolbar or status bar. Gate is `always` and overlays catalog `desktop`. chrome-pass still skips missing path-control chrome. A file browser, the iOS status bar, and a bare path word stay themselves. The marker strips. Live `data-path-control` plus toolbar-or-status-bar copy stays pending without inventing NSPathControl. Do not invent a path bar. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Outline views Don't heuristics from Apple guidance: do not put a trailing colon on an outline column heading; do not omit column headings in a multi-column outline view. Gate is `always` and overlays catalog `desktop`. chrome-pass still skips missing outline chrome. A plain list, a disclosure control, and a bare outline word stay themselves. Markers strip. Live `data-outline` plus trailing-colon copy stays pending without inventing NSOutlineView. Do not invent an outline. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- iMessage sticker Don't heuristics from Apple guidance: do not mix sizes within a single sticker pack. Gate is `always` and overlays catalog `capability:imessage`. chrome-pass still skips missing sticker-pack chrome. An image view, a plain list, a bare sticker phrase, and `MSStickerSize` stay themselves. The marker strips. Live `data-sticker-pack` plus mix-sizes copy stays pending without inventing MSSticker. Do not invent a sticker pack. Do not complete Messages. A file-size limit stays unscanned. The platform cluster stays a live-link cluster. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Action button Don't heuristics from Apple guidance: do not use an Action button label longer than three words; do not repeat the Settings guidance for the Action button. Gate is `always` and overlays catalog `phone`. chrome-pass still skips missing Action button chrome. An App Shortcut, a Home Screen quick action, and a bare Action button phrase stay themselves. Markers strip. A live `data-action-button` label longer than three words stays pending without inventing AppShortcutsProvider. Do not invent an Action button. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Camera Control Don't heuristics from Apple guidance: do not duplicate controls in the UI and the Camera Control overlay. Gate is `always` and overlays catalog `phone`. chrome-pass still skips missing Camera Control chrome. A slider, `AVCaptureSession`, and a bare Camera Control phrase stay themselves. The marker strips. Live `data-camera-control` plus duplicating-controls copy stays pending without inventing AVCaptureControl. Do not invent a Camera Control. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Dock menu Don't heuristics from Apple guidance: a custom Dock menu item must also be available in other places. Gate is `always` and overlays catalog `desktop`. chrome-pass still skips missing Dock menu chrome. A menu, a Home Screen quick action, and a bare Dock phrase stay themselves. The marker strips. A live `data-dock-menu` item whose title appears only there stays pending without inventing applicationDockMenu. Do not invent a Dock menu or a second command. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Gesture Don't heuristics from Apple guidance: do not give tap or swipe a unique meaning such as tap-to-delete with no button; do not add custom edge swipes that fight system Home or back; do not offer gesture-only navigation with no toolbar Back. Gate stays `phone,ipad`. Unknown chrome-pass stays skipped-gate. A phone host with no gesture chrome is skipped-no-affordance. A button, a tap phrase, `MagnifyGesture`, and `UIGestureRecognizer` stay themselves. Markers strip. Live `data-gesture` plus tap-to-delete copy stays pending without inventing UITapGestureRecognizer or onTapGesture. Do not invent a gesture. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Keyboard Don't heuristics from Apple guidance: do not repurpose Command-Z or Command-Q; do not add a modifier to an existing shortcut for an unrelated command; do not display help inside the keyboard; do not duplicate Emoji/Globe or Dictation keys. Gate stays `phone,ipad,desktop`. Unknown chrome-pass stays skipped-gate. A phone host with no keyboard chrome is skipped-no-affordance. An input, a text field, and a bare keyboard phrase stay themselves. Markers strip. Live `data-keyboard` plus Command-Z copy stays pending without inventing keyboardShortcut. Placeholder-only labels stay on packed forms. Scribble stays on the Pencil pack. Do not invent a keyboard. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Pointing Don't heuristics from Apple guidance: do not display instructional text with a pointer; do not create a purely decorative pointer effect. Gate stays `ipad,desktop`. Unknown chrome-pass and phone hosts stay skipped-gate. A Mac host with no pointer chrome is skipped-no-affordance. A link `cursor: pointer`, a hover rule, and a bare pointer phrase stay themselves. Markers strip. Live `data-pointer` plus instructional-text copy stays pending without inventing UIPointerStyle or NSCursor. System edge swipes stay on gestures. Pencil hover stays on the Pencil pack. Do not invent a pointer. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Pencil Don't heuristics from Apple guidance: do not use hover to initiate an action; do not use double-tap to modify content; do not distract people while they write. Gate stays `ipad+capability:pencil`. Unknown chrome-pass, phone, and Mac hosts stay skipped-gate. An iPad host with `import PencilKit` and no pencil canvas is skipped-no-affordance. `import PencilKit`, a bare Apple Pencil phrase, and `data-pe-*` markers alone stay themselves. Markers strip. Live `data-pencil` plus hover-initiates-an-action copy stays pending without inventing PKCanvasView, PKToolPicker, or UIScribbleInteraction. Pointer rules stay on pointing. Do not invent a pencil canvas. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Game controls Don't heuristics from Apple guidance: do not use abstract shapes or controller-based naming like A, X, or R1 as artwork. Gate stays `games,capability:games`. Unknown chrome-pass, phone, and Mac hosts stay skipped-gate. A game host with no game-control widget is skipped-no-affordance. `import SpriteKit`, `import GameKit`, a Game Center widget, `data-gc-*` markers, and a bare game-controller phrase stay themselves. The letter marker strips. Live `data-game-controls` plus A/X/R1 copy, and a live button labeled A, stay pending without inventing GCController or replacement artwork. A static thumbstick stays unscanned. Fake Game Center overlays stay on game-center. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- iPhone Duo Don't heuristics from Apple guidance: do not reinvent the app when it resizes; avoid fixed widths and display-specific dependencies; avoid extreme layout changes as people fold. Gate stays `duo,capability:duo`. Unknown chrome-pass and a current iPhone stay skipped-gate. A Duo host with no `data-duo` layout is skipped-no-affordance. `ArrangementView`, `reservedRegions`, `data-id-*` markers, and a bare iPhone Duo phrase stay themselves. The markers strip. Live `data-duo` plus fixed-width copy, and `UIScreen.main.bounds`, stay pending without inventing ArrangementView or a fold pose. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- CareKit Don't heuristics from Apple guidance: people don't want to see advertising in a care plan. Dedicated surface `carekit` keeps the Don't off the ResearchKit cluster. Gate stays `capability:carekit`. Unknown chrome-pass stays skipped-gate. A CareKit import with no care-plan widget is skipped-no-affordance. `import CareKit`, `import ResearchKit`, a HealthKit widget, `data-ck-ad`, and a bare CareKit phrase stay themselves. The marker strips. Live `data-carekit` plus advertising copy, and a Sponsored line, stay pending without inventing OCKCarePlanStore or a permission sheet. Health permission stays human-only. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- ResearchKit Don't heuristics from Apple guidance: don't request access to data that isn't critical to the study. Dedicated surface `researchkit` keeps the Don't off CareKit. Gate stays `capability:researchkit`. Unknown chrome-pass stays skipped-gate. A ResearchKit import with no study widget is skipped-no-affordance. `import ResearchKit`, `import CareKit`, a care-plan widget, `data-rk-critical`, and a bare ResearchKit phrase stay themselves. The marker strips. Live `data-researchkit` plus the critical-data sentence, and a "not critical to the study" line, stay pending without inventing a permission sheet or consent flow. Health permission and informed consent stay human-only. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Wallet Don't heuristics from Apple guidance: never use a change message for marketing. Dedicated surface `wallet` keeps the Don't off Apple Pay. Gate stays `capability:wallet`. Unknown chrome-pass stays skipped-gate. A PassKit import with no pass widget is skipped-no-affordance. `import PassKit`, `PKPassLibrary`, an Apple Pay button, `data-wl-marketing`, and a bare Wallet phrase stay themselves. The marker strips. Live `data-wallet` plus the change-message sentence, and a `changeMessage` sale, stay pending without inventing an Add to Wallet sheet. Decline-and-ask-again stays unscanned. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- App Clip Code Don't heuristics from Apple guidance: don't create your own App Clip Code or modify a generated one. Dedicated surface `app-clips` keeps the Don't off the platform cluster. Gate stays `capability:appclips`. Unknown chrome-pass stays skipped-gate. An App Clip entitlement with no code widget is skipped-no-affordance. The entitlement, `data-app-clip`, `data-ac-modified`, an ad sentence, and a bare App Clip phrase stay themselves. The marker strips. Live `data-app-clip-code` plus the homemade-code sentence, and a `drop-shadow` on the code, stay pending without inventing an App Clip Code. Ads, web views, and account walls stay unscanned. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- App Clip Code presentation Don't heuristics from Apple guidance: don't overlay the code with text, logos, or images; never animate or dim it; don't rotate the generated code. Same surface `app-clips` and the same `data-app-clip-code` widget. Gate stays `capability:appclips`. The new markers strip. A logo image, an animation, and a rotation on the code stay pending. A host without the capability keeps the markers. Ads stay unscanned. Do not invent an App Clip Code. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- App Clip Code aspect-ratio Don't heuristics from Apple guidance: when scaling a generated App Clip Code, don't change its aspect ratio. Same surface `app-clips` and the same `data-app-clip-code` widget. Gate stays `capability:appclips`. `data-ac-aspect` strips. `object-fit: fill` and a non-uniform `scale()` on the code stay pending. Letterbox stays on playing-video. A stretched photo stays on images. A host without the capability keeps the marker. Ads stay unscanned. Do not invent an App Clip Code. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- App Clip Code symbol Don't heuristics from Apple guidance: don't add a symbol to an App Clip Code. Same surface `app-clips` and the same `data-app-clip-code` widget. Gate stays `capability:appclips`. `data-ac-symbol` strips. A trademark glyph on the code stays pending. An image overlay stays on the overlay scanner. An SF Symbol stays on icons. A host without the capability keeps the marker. Ads stay unscanned. Do not invent an App Clip Code. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Wallet decline Don't heuristics from Apple guidance: if people decline your suggestion, don't ask them again. Same surface `wallet` and the same `data-wallet` pass widget. Gate stays `capability:wallet`. `data-wl-decline` strips. The Apple sentence and a second `Add again` after `declined` stay pending. The first Add to Wallet suggestion stays allowed. A camera "don't ask them again" sentence stays on privacy. Apple Pay stays capability-gated. Do not complete an Add to Wallet sheet. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Notification badge Don't heuristics from Apple guidance: a badge that conveys numeric information that isn't related to notifications. Same surface `notifications`. An unread count stays already-compliant. `applicationIconBadgeNumber` is not the affordance. `data-nt-badge` strips when a notification center is present. The Apple sentence and a weather value on the badge stay pending. An action-label sentence stays already-compliant. A badge with no notification center keeps the marker. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Wallet logo Don't heuristics from Apple guidance: inner drop shadows on logo artwork. Same surface `wallet` and the same `data-wallet` pass widget. Gate stays `capability:wallet`. `data-wl-shadow` strips. The Apple sentence and an inset shadow on the logo tag stay pending. An inset shadow on the pass chrome, padding on the logo, and strip-image text stay already-compliant. Apple Pay does not launch the surface. Do not invent a logo. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- Photo-editing Cancel Don't heuristics from Apple guidance: Cancel that discards edits without a confirm. Dedicated surface `photo-editing` sits before the media cluster. Gate stays `capability:photos`. `import PhotosUI` is the capability, not the widget. `data-px-cancel` strips. The Apple sentence and a session that discards edits on Cancel stay pending. No edits yet, a confirm that says edits will be lost, and a custom top toolbar sentence stay already-compliant. A host without the capability keeps the marker. `import ShazamKit` does not launch the surface. Do not invent a confirm dialog. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
- ShazamKit microphone Don't heuristics from Apple guidance: people don't expect the microphone to stay on after a recognition sample. Dedicated surface `shazamkit` sits before the media cluster. Gate stays `capability:shazam`. `import ShazamKit` is the capability, not the widget. `data-sz-mic` strips. The Apple sentence and a session that calls `.match()` and `.start()` without `.stop()` stay pending. A session that stops, and an iCloud song-library opt-in, stay already-compliant. A host without the capability keeps the marker. `import CoreML` does not launch the surface. Do not complete a microphone permission sheet. Do not overlay `always`. Design-principles Don'ts stay pending. `requiredIds` stay 12.
## Unreleased

- Live HIG URL hygiene: Toolbars (not `/navigation` or `/navigation-bars`), Gestures, Entering data; ban `/app-intents`
- Glass-safe chrome: `chrome.bars.system-materials` FAILs custom opaque bar fills; do not FAIL missing opaque nav
- Surface gates: optional ids use flat `gate`; preflight `platform` + `capabilities`; skip ≠ synthesizer drop; tech stays off `requiredIds`
- Platform gates: iPad-only `UIDeviceFamily` `[2]`; SwiftUI Mac via `.macOS(`; `multi` / `platform_secondary` include desktop for `mac-chrome`
- Wire Getting Started, foundations, patterns, inputs, system, and tech packs as gated surfaces (`requiredIds` still 12)
- One Siri worker (compose App Shortcuts); sheets compose action sheets; PassKit → wallet not Apple Pay; Control Widget gates Control Center; Pencil is iPad AND pencil
- iPhone Duo Getting started is gated (`duo` / `capability:duo`); current iPhone skips it

## 0.4.0 — 2026-09-12

`/hig` is a **framework-agnostic swarm**, not a React-only implementer.

- Detect SwiftUI / UIKit / web / React / other UI; fail only when there is no UI
- Parallel surface agents (layout, type, color, spacing, motion, controls, nav, lists/split, sheets, forms, a11y)
- Optional `hig-react` mapping skill — no component injection
- Apple canon (`knowledge/canon.md`) + `surfaces.yaml`
- Install via `npx skills add raashishah/apple-hig -g -y`

## 0.3.2 — 2026-07-25

Rename to **upgrade** (not update) and clarify README.

- `/hig upgrade` + skill `hig-upgrade` (say **HIG upgrade**)
- `scripts/upgrade-check.sh` (was update-check) + `scripts/upgrade.sh`
- README: no auto-upgrade; install shapes; how the skill “learns”
- Removes legacy `hig-update` symlink on `./setup`

## 0.3.1 — 2026-07-25

gstack-style self-upgrade (first ship; commands used the word “update”).

- Check/upgrade scripts, `VERSION`, `./setup` dual skill links

## 0.3.0 — 2026-07-25

Public packaging release for feedback.

- Cursor plugin layout (`.cursor-plugin/plugin.json`) ready for marketplace / Git install
- `./setup` installs `/hig` into `~/.cursor/skills/hig`
- Chrome grammar dry eval + brand-veto / unsupported-stack dry evals
- Proof notes from Pink Depot dogfood + admissionsdemo portable run
- Honest gaps list (next chrome FAIL candidates) for early testers

## 0.2.0

- Bare `/hig` = design + implement from existing requirements
- Pattern packs + brand veto + materials arbitration
- Chrome grammar v1 (`chrome.view-mode.icons`, toolbar budget, filter density, form column cohesion, sidebar collapse)
