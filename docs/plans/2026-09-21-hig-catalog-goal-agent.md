---
title: HIG catalog goal agent - Plan
type: feat
date: 2026-09-21
artifact_contract: ce-unified-plan/v1
artifact_readiness: implementation-ready
product_contract_source: ce-plan-bootstrap
execution: knowledge-work
jev: jev-1.13.0
---

# HIG catalog goal agent - Plan

Locked-lane catalog plan. It does **not** change apply on main until merge. PR #12 now carries chrome, host fonts, catalog inventory, and the after-chrome goal loop. Jev `plan_doc_shape=one_page_locked_lane` (0.98).

Jev (`jev-1.13.0`): `apply_ssot=surfaces_yaml_requiredIds_12` (1.0); `catalog_loop_relation=additive_later` (1.0, U2 now landed additive on this PR); `fonts_this_pr=leave_host` (1.0). Remove `requiredIds` later noul 0.17. Watch/TV/Vision now noul 0.08. Lists on Vue/Flutter noul 0.87. TypeSafe is not a `/hig` runtime. Jev `next_unit=finish_eval_docs_commit` (0.53); `mark_goal_complete_after_u2` noul 0.08.

## Locked apply (already on main)

- Apply SSOT is `skills/hig/knowledge/surfaces.yaml`.
- `requiredIds` stay 12 (layout … accessibility). Round 1 apply is that set until `check-chrome.mjs` is P0-clean.
- Optional surfaces stay gated. iPhone Duo is `gs-iphone-duo` (`duo` / `capability:duo`), not required.
- `registry.yaml` is rare-leaf Apple URLs only. Do not revive it as the apply catalog.
- Watch / TV / Vision stay out.
- Packs for foundations, patterns, components, inputs, Mac, games, and tech are already on main via #10.
- Human-only: Apple Pay capture, Sign in with Apple consent, biometrics. Style chrome around those sheets; do not complete them.

## This PR (#12)

Mechanical chrome gate + recipes. Host fonts stay (no SF Pro / `-apple-system` rewrite as success). Framework-agnostic design rules map onto the host stack. Any-model round 1 remains the 12 `requiredIds`. U1 catalog inventory (`knowledge/catalog.yaml`) is additive and is not the apply SSOT. U2 goal loop (`scripts/plan-catalog.mjs`) runs after chrome P0: remaining packed applicable topics, persist `.hig/catalog-status.yaml`, stop when remaining is 0. U4 host-pattern skip (`skipped-no-affordance`). U5 same chrome P0 on web + Swift hosts. Catalog `failWhen` derived at load from grammar + pack Do/Don't. Mechanical apply (`apply-chrome.mjs`) for every grammar ID. Catalog apply (`apply-catalog.mjs`) accounts chrome-backed packed topics whose pack has no Don't bullets, pack Don't code spans, and required-surface prose Don'ts. Optional widget affordances skip missing menus/pickers/progress/search/notifications/loading/feedback/onboarding/drag/settings/undo. Writing Don'ts account when every bullet has a scanner. Chrome-backed layout/materials/lists/forms/navigation Don'ts account when every bullet has a scanner. Visual any-host Apple-ness is still unproven.

## Additive catalog loop

Ingest Apple's **live** HIG index into derived records (`id`, URL, framework-free `failWhen` / `passWhen`, `appliesWhen`). No frozen topic count. Refresh on `/hig upgrade`.

Wave 0 stays the 12 `requiredIds` until required chrome is P0-clean. Then apply **matching** catalog topics (host has the pattern or platform). Lists apply on Vue/Flutter. Complications do not apply to a web host. Skip unmatched Watch/TV/Vision.

Do not delete `requiredIds`. Do not replace `surfaces.yaml`. Keep `check-chrome.mjs` as P0 for solved IDs. Jev classifies topics at plugin-build time only.

## Do not hardcode (later loop)

| Forbidden | Instead |
|---|---|
| Topic count as a pass integer | Diff against the live Apple index |
| Font stacks as success | Host face stays; type rules are hierarchy, ramp, roles, readability |
| Framework names as `passWhen` | Design rule; mapping from detected stack |
| JS copies of yaml id lists | Load ids from `grammar.yaml` / catalog records |

Playbook data (Apple URL + design rule) is allowed. Frozen policy in code is not.

## Sequence

1. U1. Ingest live Apple index → derived catalog records. Eval tracks the index, not a number. **Landed on this PR.**
2. U2. Goal loop **after** wave-0 `requiredIds`. Applicable set is extra work, not a replacement stop condition. **Landed on this PR** (`plan-catalog.mjs` + `eval/run-catalog.mjs` loop cases).
3. U3. Host fonts already left alone on #12. Keep that.
4. U4. `appliesWhen` from host patterns (scan list, form, overlay, chrome), not a framework enum. **Landed on this PR** (`affordance` on `surfaces.yaml`; `skipped-no-affordance`).
5. U5. Same design FAIL proven on two detected stacks without naming those stacks in the rule. **Landed on this PR** (`chrome-antipatterns-web` + `chrome-antipatterns-swift`).
6. Catalog `failWhen` / `passWhen` derived at load from `grammar.yaml` + pack Do/Don't. **Landed on this PR.** Unpackaged rows stay stubs.
7. Mechanical apply for the two dual-stack P0s (`apply-chrome.mjs`) so any model does not have to invent those recipes. **Landed on this PR.**
8. Mechanical apply for remaining P0 grammar IDs (and `nested-cards`). **Landed on this PR.**
9. Mechanical apply for remaining P1 IDs (filter-density, sidebar). **Landed on this PR.** `apply-chrome.mjs` owns every grammar ID.
10. Catalog apply accounts chrome-backed packed topics (`apply-catalog.mjs`). **Landed on this PR.** Pack-only pending topics remain.
11. Pack **Chrome gates** join catalog `chromeIds`; pack **Don't** code spans apply/account. **Landed on this PR.** Prose-only Don'ts stay pending.
12. Required-surface prose Don't heuristics (type, color, motion, a11y). Account only when every Don't is mechanically checkable. **Landed on this PR.** Optional packed Don'ts stay pending.
13. Optional widget affordance skip (menu, picker, progress, search, notification, loading, feedback, onboarding, drag). **Landed on this PR.** Hosts that have the widget still need Don't apply.
14. Search Don't heuristics. **Landed on this PR.** Account search-fields/searching when every Don't is checkable. Do not invent a browse list.
15. Settings and undo skip-unless affordance. **Landed on this PR.** Do not invent Settings or Undo.
16. Writing Don't heuristics. **Landed on this PR.** Account writing when every Don't is checkable. Do not invent the next step or rewrite brand voice.
17. Privacy Don't heuristics. **Landed on this PR.** Account privacy when every Don't is checkable. Do not invent a decline control.
18. Branding Don't heuristics. **Landed on this PR.** Account branding when every Don't is checkable. Do not inject a brand kit. Other foundation Don'ts wait.
19. Icons / images / app-icons / inclusion Don't heuristics. **Landed on this PR.** Account those topics when every Don't is checkable. Do not inject an icon kit or invent inclusive copy. Design-principles Don'ts stay pending (pack-authoring: Watch/TV/Vision coverage, house taste).
20. Optional-widget Don't heuristics (settings, undo, loading, feedback, onboarding, drag, notifications, launching). **Landed on this PR.** Account those topics when every Don't is checkable and the host has that widget. Skip-unless affordance stays. Do not invent a missing widget. Menus/pickers/progress still have no Don't section. Design-principles Don'ts stay pending.
21. Chrome-backed Don't heuristics (layout, materials, lists, forms, navigation). **Landed on this PR.** Account those topics when every Don't is checkable. Packs with Don't bullets account through Don't scanners, not clean chromeIds. Do not fake-account design-principles. Visual any-host Apple-ness is still unproven.
22. Host-widget Don't heuristics (menus, pickers, progress, controls). **Landed on this PR.** Account those topics when every Don't is checkable and the host has that widget. Skip-unless affordance stays. Do not invent a missing menu, picker, field, or route. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
23. System-chrome Don't heuristics (widgets, Live Activities, status bars, Control Center). **Landed on this PR.** Account those topics when every Don't is checkable and the host has that capability. Capability skip stays. Do not invent a widget kit. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
24. RTL Don't heuristics (right-to-left). **Landed on this PR.** Account that topic when every Don't is checkable. Whole-window `scaleX(-1)`, physical `margin-left`/`padding-right`, `dir="auto"` on the locale root, and `chevron.left` back controls rewrite to logical. Unicode `←` back chevrons stay pending. Do not invent a mirrored design file. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
25. Nested-modal Don't heuristics (sheets, alerts, action-sheets, modality). **Landed on this PR.** Account those topics when every Don't is checkable and the host has an overlay. Skip-unless overlay affordance stays. `data-nested-modal` strips. Nested `<dialog>` stacks stay pending without inventing a sheet. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
26. Composed `also:` catalog wiring. **Landed on this PR.** Dark Mode, SF Symbols, context/pull-down/pop-up menus, and notifications glanceability ride existing complete Don't packs. No second swarm lease. The Mac menu bar and tab-views stay unpackaged. Watch/TV/Vision stay unpackaged. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
27. Slider, scroll-view, and popover packs. **Landed on this PR.** Account those topics when every Don't has a scanner and the host has that widget. Skip-unless `slider` / `scroll` / `popover` affordances stay. Volume sliders, same-axis nested overflow, and compact/cascade/warning popovers scan from Apple pages. Do not invent a missing slider, scroll pane, or popover. Do not map sliders onto pickers, document scroll onto scroll-views, or sheets onto popovers. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
28. Collections pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a collection widget. Skip-unless `collection` affordance stays. Custom confusing layouts, text collections that should be tables, and overlapping items scan from Apple's collections page. Do not invent a missing collection. Do not map collections onto lists: a `<ul>` inventory stays a list; a visual row/grid (`data-collection`, `UICollectionView`, `LazyVGrid`) is a collection. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
29. Page-controls pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a page-control widget. Skip-unless `pagecontrol` affordance stays. Hierarchical/nonsequential pages, more than about 10 dots, more than two indicator images, and colored indicators scan from Apple's page-controls page. Do not invent a missing page control. Do not map page controls onto progress bars, tab bars, or numbered pagination. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
30. Labels pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a static label widget. Skip-unless `label` affordance stays. Editable labels, large-amount labels, and uncopyable useful text scan from Apple's labels page. Do not invent a missing label. Do not map labels onto form `<label>`, `aria-label`, or every string. Host fonts stay. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
31. Text-views pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a text-view widget. Skip-unless `textview` affordance stays. Short text views that belong in a label or field, and uncopyable useful text, scan from Apple's text-views page. Do not invent a missing text view. Do not map text views onto `<input>`, form `<label>`, or document body. Host fonts stay. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
32. Image-views pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has an image-view widget. Skip-unless `imageview` affordance stays. Button behaviors, interface-icon image views, and text overlays scan from Apple's image-views page. Do not invent a missing image view. Do not map image views onto every `<img>` or SF Symbol. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
33. Charts pack. **Landed on this PR.** Account Charts and Charting data when every Don't has a scanner and the host has a chart widget. Skip-unless `chart` affordance stays. Color-only series, hover-only critical values, charts that should be tables, and overcrowded plots scan from Apple's Charts and Charting data pages. `charting-data` rides the same pack via `also:`. Do not invent a missing chart. Do not map charts onto `<table>` or decorative `<svg>`. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
34. Disclosure-controls pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a disclosure widget. Skip-unless `disclosure` affordance stays. Extra disclosure buttons, unlabeled triangles, and unhidden advanced details scan from Apple's disclosure-controls page. Do not invent a missing disclosure control. Do not map disclosure onto a menu, picker, or `aria-expanded` toolbar. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
35. Boxes pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a dedicated box widget. Skip-unless `box` affordance stays. Nested boxes and oversized boxes scan from Apple's boxes page. Distinct from `chrome.ive.nested-cards` (list/form extra card panels). Do not invent a missing box. Do not map boxes onto cards, grouped lists, or form fieldsets. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
36. Edit-menus pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a real edit menu. Skip-unless `editmenu` affordance stays. Custom duplicates, Cut/Copy with no selection, and redundant edit controls scan from Apple's edit-menus page. Do not invent Cut, Copy, or Paste. Do not map edit menus onto a command `role="menu"` or the Mac menu bar. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
37. Offering-help pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a tip, tooltip, or help overlay. Skip-unless `help` affordance stays. Wrong-platform verbs, standard-component tutorials, and promotional tips scan from Apple's offering-help page. Do not invent help. Do not map offering-help onto onboarding or a `title=` attribute. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
38. Web-views pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has an embedded web view. Skip-unless `webview` affordance stays. Missing forward/back on multi-page embeds and Safari replicas scan from Apple's web-views page. Do not invent Back or Forward. Do not map web-views onto the host document. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
39. Activity-views pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a share sheet. Skip-unless `activityview` affordance stays. Duplicate common actions and alternative reveal controls scan from Apple's activity-views page. Do not invent a Share button. Do not map activity-views onto a Share nav link. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
40. Printing pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a dedicated Print action. Skip-unless `print` affordance stays. Print-when-nothing-printable and duplicate page-orientation scan from Apple's printing page. Do not invent a printer or dimming. Do not map printing onto a share-sheet Print row. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
41. Going-full-screen pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a dedicated full-screen experience. Skip-unless `fullscreen` affordance stays. Programmatic resize, auto-exit, and custom window-mode menus scan from Apple's going-full-screen page. Do not invent a window. Do not map full-screen onto `100vh` or a video's native controls. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
42. File-management pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a document browser. Skip-unless `filebrowser` affordance stays. Custom top toolbars, extensions shown by default, and explicit-save-required scan from Apple's file-management page. Do not invent a file browser or autosave. Do not map file-management onto `<input type="file">`. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
43. Focus-and-selection pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a dedicated focus system. Skip-unless `focus` affordance stays. Steal-focus and custom focus effects scan from Apple's focus-and-selection page. Do not invent a focus ring. Do not map focus onto `:focus` CSS, `tabindex`, or `autofocus` alone. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
44. Managing-accounts pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has dedicated account or sign-in chrome. Skip-unless `account` affordance stays. Forced sign-in walls, buried deletion, and passcode-for-account-auth scan from Apple's managing-accounts page. Do not invent a guest path, a deletion flow, or Face ID. Do not map managing-accounts onto a form, Settings, a password field, or Sign in with Apple. Face ID and Sign in with Apple consent stay human-only. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
45. Tab-views pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has an NSTabView-style tabbed pane. Skip-unless `tabview` affordance stays. Pop-up tab switchers, more than six tabs, and cross-pane controls scan from Apple's tab-views page. Do not invent a tab strip or collapse extras into a menu. Do not map tab-views onto packed tab-bars. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
46. Multitasking pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has dedicated multitasking or Picture in Picture chrome. Skip-unless `multitask` affordance stays. Pause-on-switch, spare-notification, and primary-audio-interrupt scan from Apple's multitasking page. Do not invent a `visibilitychange` listener, `.pause()`, or a notification. Do not map multitasking onto a video element, `100vh`, or going-full-screen. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
47. Ratings-and-reviews pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a ratings prompt. Skip-unless `reviewprompt` affordance stays. First-launch, interrupt-a-task, and pestering repeated requests scan from Apple's ratings-and-reviews page. Do not invent StoreKit, a delay, or a custom review sheet. Do not map ratings-and-reviews onto a star glyph, packed rating-indicators, or onboarding. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
48. Windows pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a dedicated app window. Skip-unless `appwindow` affordance stays. Default new windows, custom frames, calling a window a scene, and critical bottom-bar actions scan from Apple's windows page. Do not invent a frame, a title bar, or an inspector. Do not map windows onto the host document, `UIWindow`, `100vh`, or going-full-screen. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
49. Playing-video pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a video player. Skip-unless `videoplayer` affordance stays. A diverging custom player, letterbox/pillarbox padding, a resume prompt, and a loading or splash screen before play scan from Apple's playing-video page. Do not invent a player, autoplay, or a Space handler. Do not map playing-video onto `100vh` or packed multitasking. Mixed-audio-on-mode-switch stays on packed multitasking. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
50. Playing-haptics pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a haptic API. Skip-unless `haptic` affordance stays. Wrong-meaning system patterns, overused haptics, and haptics with no off switch scan from Apple's playing-haptics page. Do not invent Core Haptics or a mute toggle. Do not map playing-haptics onto a button tap. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
51. AirPlay pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has AirPlay routing. Skip-unless `airplay` affordance stays. Stop-on-background, interrupting another app's playback, automatic mirroring, and background loops scan from Apple's AirPlay page. Do not invent a route picker, ambient playback, or a keep-playing handler. Do not map AirPlay onto every `<video>` or a Share control. Prefer-system-player stays on packed playing-video. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
52. Gyro-and-accelerometer pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a motion API. Skip-unless `gyro` affordance stays. Motion without a tangible benefit and direct interface manipulation outside gameplay scan from Apple's gyro-and-accelerometer page. Do not invent Core Motion or a gameplay mode. Do not map gyro onto a swipe, a scroll view, or CSS motion. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
53. Home Screen quick-actions pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has Home Screen quick actions. Skip-unless `quickaction` affordance stays. App name in a title and emoji-in-place-of-a-symbol scan from Apple's home-screen-quick-actions page. Do not invent a Home Screen menu or an SF Symbol. Do not map quick actions onto a context menu, Share, or packed App Shortcuts. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
54. Live-viewing-apps pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has dedicated live-viewing chrome. Skip-unless `liveviewing` affordance stays. Unmarked live vs VOD and audio that continues after leaving the live tab scan from Apple's live-viewing-apps page. Do not invent a live badge, a Watch Now control, or an EPG. Do not map live-viewing onto every `<video>` or packed playing-video. Mixed-audio-on-mode-switch stays on packed multitasking. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
55. Snippets pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a Siri or App Shortcut snippet. Skip-unless `snippet` affordance stays. Spoken dialogue as visual purpose and a custom view taller than 400 points scan from Apple's snippets page. Do not invent a snippet or a max-height. Do not map snippets onto a card, a notification, or packed App Shortcuts. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
56. Generative-AI pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has a generative feature. Skip-unless `genai` affordance stays. Packed `gate: always` overlays catalog `capability:genai`. AI content presented as human-authored and generated content with no dismiss/revert/retry scan from Apple's generative-ai page. Do not invent a generative feature, AI-disclosure copy, Undo, or Retry. Do not map generative-ai onto a textarea, packed inclusion, or packed privacy. The media-intelligence cluster stays a live-link cluster. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
57. Always-On pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has Always On chrome. Skip-unless `alwayson` affordance stays. Packed `gate: always` overlays catalog `capability:alwayson`. Sensitive information left visible and motion that stops instantly scan from Apple's always-on page. Do not invent Always On, redaction, or a motion rest. Do not map always-on onto dimmed CSS, packed Live Activities, or packed widgets. The platform-tech cluster stays a live-link cluster. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
58. SharePlay pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has SharePlay chrome. Skip-unless `shareplay` affordance stays. Packed `gate: always` overlays catalog `capability:shareplay`. SharePlay paired with an adjective and SharePlayed / SharePlays / SharePlaying scan from Apple's shareplay page. Do not invent SharePlay or rewrite brand copy. Do not map shareplay onto a Share control, packed activity-views, or packed multitasking. The icloud-shareplay cluster stays a live-link cluster. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
59. Nearby-interactions pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has Nearby Interaction chrome. Skip-unless `nearby` affordance stays. Catalog appliesWhen is already `always`. Nearby as the only way to perform a task and explicit portrait-hold instructions scan from Apple's nearby-interactions page. Do not invent Nearby Interaction, a fallback control, or portrait coaching. Do not map nearby-interactions onto a Share control, NFC, geolocation, or packed AirPlay. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
60. Activity-rings pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has an Activity ring widget. Skip-unless `activityring` affordance stays. Packed `gate: always` overlays the watch slug. Other types of data, more than one person, recolored rings, and decoration or branding scan from Apple's activity-rings page. Do not invent Activity rings, a black background, or ring colors. Do not map activity-rings onto a progress bar, a CSS circle, or packed notifications. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
61. NFC pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has NFC chrome. Skip-unless `nfc` affordance stays. Packed `gate: always` overlays catalog `capability:nfc`. Tap-or-touch scan instructions and NFC / Core NFC / Near-field communication / NFC tag jargon scan from Apple's NFC page. Do not invent NFC, a scanning sheet, or rewritten scan copy. Do not complete an NFC read. Do not map nfc onto Nearby Interaction, Wallet, Tap to Pay, or packed Apple Pay. The commerce cluster stays a live-link cluster (`covers: [iap, wallet]`). The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
62. Augmented-reality pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has AR chrome. Skip-unless `ar` affordance stays. Packed `gate: always` overlays catalog `capability:arkit`. World detection / Adjust tracking / plane-to-anchor copy and altered or non-ARKit glyph/badge use scan from Apple's augmented-reality page. Do not invent ARKit, a reset, or a coaching view. Do not map augmented-reality onto every image, CSS 3D, Nearby Interaction, or NFC. The media-intelligence cluster stays a live-link cluster. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
63. Tap to Pay pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has Tap to Pay chrome. Skip-unless `taptopay` affordance stays. Packed `gate: always` overlays catalog `capability:taptopay`. Apple logo in a Tap to Pay button and Tap to Pay / Tap to Pay on iPhone on a Look Up, Store Card, Verify, Refund, or loyalty button scan from Apple's tap-to-pay-on-iphone page. Do not invent Tap to Pay, a payment sheet, or rewritten button labels. Do not complete a payment. Do not map tap-to-pay-on-iphone onto NFC, Wallet, or packed Apple Pay. The commerce cluster stays a live-link cluster (`covers: [iap, wallet]`). The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
64. ID Verifier pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has ID Verifier chrome. Skip-unless `idverifier` affordance stays. Packed `gate: always` overlays catalog `capability:idverifier`. Apple logo in an ID Verifier button and NFC or QR communication symbols on a Verify Age or Verify Identity button scan from Apple's id-verifier page. Do not invent ID Verifier, a verification sheet, or rewritten button labels. Do not complete a verification. Do not map id-verifier onto NFC, Tap to Pay, Wallet, or packed Apple Pay. The commerce cluster stays a live-link cluster (`covers: [iap, wallet]`). The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
65. In-app purchase pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has IAP chrome. Skip-unless `iap` affordance stays. Packed `gate: always` overlays catalog `capability:iap`. Replica confirmation sheets, buried refund-request buttons, and characterized Apple refund policies scan from Apple's in-app-purchase page. Do not invent IAP, a confirmation sheet, or rewritten refund copy. Do not complete a purchase. Do not map apple-in-app-purchase onto packed Apple Pay, Tap to Pay, Wallet, or ID Verifier. The commerce cluster stays a live-link cluster (`covers: [iap, wallet]`). The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
66. Maps pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has MapKit chrome. Skip-unless `map` affordance stays. Packed `gate: always` overlays catalog `capability:mapkit`. Covered Maps legal links and indoor maps that replicate Apple Maps scan from Apple's maps page. Do not invent a map, attribution, or indoor tiles. Do not map maps onto packed CarPlay, a generic map word, or an iframe. The carplay-maps cluster stays a live-link cluster (`covers: [carplay]`); dedicated maps attaches by appleUrl. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
67. HomeKit pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has HomeKit chrome. Skip-unless `homekit` affordance stays. Packed `gate: always` overlays catalog `capability:homekit`. Company names or model numbers as Siri service names, HomeKit database overwritten without direction, duplicate home settings, and blocked camera images scan from Apple's homekit page. Do not invent HomeKit, a pairing sheet, or a replica Home app. Do not pack require-account. Do not map homekit onto packed iCloud, a generic Home app word, or packed managing-accounts. The icloud-shareplay cluster stays a live-link cluster (`covers: [icloud, shareplay]`); dedicated homekit attaches by appleUrl. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
68. Workouts pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has workout chrome. Skip-unless `workout` affordance stays. Catalog appliesWhen is already `always`. Distracting chrome during an active workout and extremely brief sessions recorded as workouts scan from Apple's workouts page. Do not invent a workout session, a metrics page, or a replica of Workout. Do not pack the Activity-rings bullet. Do not map workouts onto packed Activity rings, packed HealthKit, or a progress bar. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
69. Live Photos pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has Live Photo chrome. Skip-unless `livephoto` affordance stays. Packed `gate: always` overlays the cluster appliesWhen. Frames or audio presented separately, a video playback button on a Live Photo, and a replicated Live Photos experience in an unsupported environment scan from Apple's live-photos page. Do not invent PHLivePhotoView, a playback button, or a replica experience. Do not map live-photos onto every still photo, every `<video>`, packed playing-video, or a bare Live Photo phrase. The media-intelligence cluster stays a live-link cluster; dedicated live-photos attaches by appleUrl (first-wins). Do not steal photo-editing, ShazamKit, or machine-learning via the cluster appleUrl. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
70. iCloud pack. **Landed on this PR.** Account that topic when every Don't has a scanner and the host has iCloud chrome. Skip-unless `icloud` affordance stays. Packed `gate: always` overlays catalog `capability:icloud`. Asking which documents to keep in iCloud, alerting when iCloud is unavailable, and storing app resources in iCloud scan from Apple's icloud page. Do not invent iCloud, a keep-in-iCloud picker, or an unavailable alert. Do not map icloud onto packed file-management, packed SharePlay, or a bare iCloud phrase. The icloud-shareplay cluster stays a live-link cluster; dedicated icloud attaches by appleUrl (first-wins). Do not steal SharePlay, AirPlay, or HomeKit via the cluster appleUrl. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
71. Siri pack. **Landed on this PR.** Account leftover Siri Don'ts when every Don't has a scanner and the host has Siri chrome. Skip-unless `siri` affordance stays. Packed `gate: always` overlays catalog `capability:siri`. Advertisements or IAP pitches in Siri-delivered content, impersonated Siri or reserved phrases, and Siri referred to with she, he, or her scan from Apple's siri page. Do not invent Siri, a Siri sheet, or App Intents. Do not complete Siri permission, voice enrollment, or Lock Screen auth. Do not map siri onto packed App Shortcuts, packed snippets, packed inclusion, or a bare Siri / Hey Siri phrase. The siri-app-shortcuts cluster keeps app-shortcuts via `also:`; dedicated siri attaches by appleUrl (first-wins). Do not steal app-shortcuts. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
72. App Shortcuts leftover Don'ts. **Landed on this PR.** Account leftover App Shortcuts copy Don'ts on the existing `siri-app-shortcuts` cluster when every Don't has a scanner and the host has App Shortcuts chrome. Skip-unless `appshortcut` affordance stays. Packed `gate: always` overlays catalog `capability:siri`. Do not add a second exclusive `app-shortcuts` surface. Re-skin Siri or Shortcuts editor chrome, App Shortcuts or Shortcuts written in lowercase, and individual shortcuts written in title case scan from Apple's app-shortcuts page. Do not invent App Intents, a Siri sheet, or a Shortcuts editor. Do not map app-shortcuts onto packed Siri, packed snippets, or a bare App Shortcuts phrase. Dedicated siri stays itself. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
73. HealthKit leftover Don'ts. **Landed on this PR.** Account leftover HealthKit Don'ts when every Don't has a scanner and the host has HealthKit chrome. Skip-unless `healthkit` affordance stays. Packed `gate: always` overlays catalog `capability:healthkit`. Custom screens that replicate the Health permission screen, in-app screens that manage health data sharing, and the term HealthKit in user-facing copy scan from Apple's healthkit page. Do not invent HealthKit, a permission replica, or a sharing screen. Do not complete Health permission, biometrics, or Medical ID. Do not map healthkit onto packed HomeKit, packed Activity rings, packed workouts, packed privacy, or a bare HealthKit phrase. Activity-ring Don'ts stay packed. CareKit and ResearchKit stay on the live-link cluster. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
74. CarPlay leftover Don'ts. **Landed on this PR.** Account leftover CarPlay Don'ts when every Don't has a scanner and the host has CarPlay chrome. Skip-unless `carplay` affordance stays. Packed `gate: always` overlays catalog `capability:carplay,capability:mapkit`. CarPlay locked out because iPhone requires input, errors reported on iPhone instead of CarPlay, and iPhone interactions required while CarPlay is active scan from Apple's carplay page. Do not invent CarPlay, a driving template, or an iPhone lock sheet. Do not complete vehicle pairing. Do not map carplay onto packed maps or a bare CarPlay phrase. Dedicated carplay attaches by appleUrl (first-wins). The carplay-maps cluster stays a live-link cluster (`covers: [carplay]`). Dedicated maps stays itself. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
76. Apple Pay leftover Don'ts. **Landed on this PR.** Account leftover Apple Pay Don'ts when every Don't has a scanner and the host has Apple Pay chrome. Skip-unless `applepay` affordance stays. Gate stays `capability:applepay`. Do not overlay `always`, because a PassKit wallet host must not launch apple-pay. The Apple Pay mark used as a payment button, Apple Pay made plural or possessive, and the Apple logo used in place of the word Apple scan from Apple's apple-pay page. Do not invent Apple Pay, a payment sheet, or a logo. Do not complete the payment sheet. Do not map apple-pay onto packed Wallet, packed Tap to Pay, packed NFC, packed In-App Purchase, or a bare Apple Pay phrase. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
75. Sign in with Apple leftover Don'ts. **Landed on this PR.** Account leftover Sign in with Apple Don'ts when every Don't has a scanner and the host has Sign in with Apple chrome. Skip-unless `siwa` affordance stays. Packed `gate: always` overlays catalog `capability:signinwithapple`. A password asked for alongside Sign in with Apple, a personal email asked for when a private relay address is used, and a custom Apple logo on the button scan from Apple's sign-in-with-apple page. Do not invent Sign in with Apple, a password field, or a logo. Do not complete the system sign-in sheet. Do not map sign-in-with-apple onto packed managing-accounts, packed Apple Pay, or a bare Sign in with Apple phrase. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

77. Playing audio Don'ts. **Landed on this PR.** Account playing-audio Don'ts when every Don't has a scanner and the host has playing-audio chrome. Skip-unless `audioplayer` affordance stays. Gate is `always`. The app setting the system output volume, audio controls repurposed for a non-playback action, and playback that continues after headphones disconnect scan from Apple's playing-audio page. Do not invent a player, a volume view, or a headphone sheet. Do not map playing-audio onto a volume slider, a video element, packed AirPlay, or a bare audio phrase. Volume-slider Don'ts stay on packed sliders. Video playback stays on packed playing-video. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

78. Game Center leftover Don'ts. **Landed on this PR.** Account leftover Game Center Don'ts when every Don't has a scanner and the host has Game Center chrome. Skip-unless `gcaccess` affordance stays. Gate stays `capability:gamecenter`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. The access point shown during active gameplay, official artwork resized or restyled, and custom links that say GameKit, game center, Awards, or Rankings scan from Apple's game-center page. Do not invent an access point, a Game Overlay, or a sign-in sheet. Do not complete Game Center authentication. Do not map game-center onto a bare Game Center phrase or import GameKit. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

79. Panels Don'ts. **Landed on this PR.** Account panels Don'ts when every Don't has a scanner and the host has panel chrome. Skip-unless `panel` affordance stays. Gate is `always` and overlays catalog `desktop`. A panel listed in the Window menu documents list, a minimize button on a panel, and a HUD that obscures the content it adjusts scan from Apple's panels page. Do not invent a panel, a minimize button, or a Window menu. Do not map panels onto a sheet, a regular window, `data-window`, or `NSOpenPanel`. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

80. Path controls Don'ts. **Landed on this PR.** Account path-controls Don'ts when every Don't has a scanner and the host has path-control chrome. Skip-unless `pathcontrol` affordance stays. Gate is `always` and overlays catalog `desktop`. A path control placed in a toolbar or status bar scans from Apple's path-controls page. Do not invent a path bar. Do not map path-controls onto a file browser, the iOS status bar, or a bare path word. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

81. Outline views Don'ts. **Landed on this PR.** Account outline-views Don'ts when every Don't has a scanner and the host has outline chrome. Skip-unless `outline` affordance stays. Gate is `always` and overlays catalog `desktop`. A trailing colon on an outline column heading and a multi-column outline view with no column headings scan from Apple's outline-views page. Do not invent an outline. Do not map outline-views onto a plain list, a disclosure control, or a bare outline word. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

82. iMessage sticker Don'ts. **Landed on this PR.** Account imessage-apps-and-stickers Don'ts when every Don't has a scanner and the host has sticker-pack chrome. Skip-unless `stickerpack` affordance stays. Gate is `always` and overlays catalog `capability:imessage`. Mixed sizes within a single sticker pack scan from Apple's imessage-apps-and-stickers page. A file-size limit is not source-measurable, so it stays unscanned. Do not invent a sticker pack or complete Messages. Do not map imessage-apps-and-stickers onto an image view, a plain list, a bare sticker phrase, or `MSStickerSize`. Dedicated imessage-apps-and-stickers attaches by appleUrl. The platform cluster stays a live-link cluster. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

83. Action button Don'ts. **Landed on this PR.** Account action-button Don'ts when every Don't has a scanner and the host has Action button chrome. Skip-unless `actionbutton` affordance stays. Gate is `always` and overlays catalog `phone`. An Action button label longer than three words, and content that repeats the Settings guidance for the Action button, scan from Apple's action-button page. Do not invent an Action button or a shorter label. Do not map action-button onto an App Shortcut, a Home Screen quick action, or a bare Action button phrase. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

84. Camera Control Don'ts. **Landed on this PR.** Account camera-control Don'ts when every Don't has a scanner and the host has Camera Control chrome. Skip-unless `cameracontrol` affordance stays. Gate is `always` and overlays catalog `phone`. Duplicating controls in the UI and the Camera Control overlay scans from Apple's camera-control page. Do not invent a Camera Control. Do not map camera-control onto a slider, `AVCaptureSession`, or a bare Camera Control phrase. Custom symbols stay on packed SF Symbols. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

85. Dock menu Don'ts. **Landed on this PR.** Account dock-menus Don'ts when every Don't has a scanner and the host has Dock menu chrome. Skip-unless `dockmenu` affordance stays. Gate is `always` and overlays catalog `desktop`. A custom Dock menu item that is not available in other places scans from Apple's dock-menus page. Do not invent a Dock menu or a second command. Do not map dock-menus onto a menu, a Home Screen quick action, or a bare Dock phrase. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

86. Gesture Don'ts. **Landed on this PR.** Account gestures Don'ts when every Don't has a scanner and the host has gesture chrome. Skip-unless `gesture` affordance stays. Gate stays `phone,ipad`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. Unique tap or swipe meaning, custom edge swipes that fight system Home or back, and gesture-only navigation with no toolbar Back scan from the existing gestures pack. Do not invent a gesture recognizer. Do not map gestures onto a button, a tap phrase, `MagnifyGesture`, or `UIGestureRecognizer`. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

87. Keyboard Don'ts. **Landed on this PR.** Account keyboards and virtual-keyboards Don'ts when every Don't has a scanner and the host has keyboard chrome. Skip-unless `keyboard` affordance stays. Gate stays `phone,ipad,desktop`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. Repurposed Command-Z or Command-Q, a modifier added to an existing shortcut for an unrelated command, help inside the keyboard, and duplicated Emoji/Globe or Dictation keys scan from Apple's keyboards and virtual-keyboards pages. Placeholder-only labels stay on packed forms. Scribble stays on the Pencil pack. Do not invent a keyboard. Do not map keyboards onto an input, a text field, or a bare keyboard phrase. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

88. Pointing Don'ts. **Landed on this PR.** Account pointing-devices Don'ts when every Don't has a scanner and the host has pointer chrome. Skip-unless `pointer` affordance stays. Gate stays `ipad,desktop`. Do not overlay `always`, because unknown chrome-pass and phone hosts must stay skipped-gate. Instructional text displayed with a pointer, and a purely decorative pointer effect, scan from Apple's pointing-devices page. Do not invent a pointer. Do not map pointing-devices onto a link `cursor: pointer`, a hover rule, or a bare pointer phrase. System edge swipes stay on gestures. Pencil hover stays on the Pencil pack. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

89. Pencil Don'ts. **Landed on this PR.** Account apple-pencil-and-scribble Don'ts when every Don't has a scanner and the host has pencil chrome. Skip-unless `pencil` affordance stays. Gate stays `ipad+capability:pencil`. Do not overlay `always`, because unknown chrome-pass, phone, and Mac hosts must stay skipped-gate. Hover that initiates an action, double-tap that modifies content, and distraction while people write scan from Apple's apple-pencil-and-scribble page. Do not invent a pencil canvas. Do not map apple-pencil-and-scribble onto `import PencilKit`, a bare Apple Pencil phrase, or `data-pe-*` markers alone. Pointer instructional text stays on pointing. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

90. Game controls Don'ts. **Landed on this PR.** Account game-controls Don'ts when every Don't has a scanner and the host has game-control chrome. Skip-unless `gamecontrol` affordance stays. Gate stays `games,capability:games`. Do not overlay `always`, because unknown chrome-pass, phone, and Mac hosts must stay skipped-gate. Abstract shapes or controller-based naming like A, X, or R1 as artwork scans from Apple's game-controls page. A static thumbstick is opt-to, so it stays unscanned. Do not invent a controller or replacement artwork. Do not map game-controls onto `import SpriteKit`, `import GameKit`, a Game Center widget, `data-gc-*` markers, or a bare game-controller phrase. Fake Game Center overlays stay on game-center. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

91. iPhone Duo Don'ts. **Landed on this PR.** Account designing-for-iphone-duo Don'ts when every Don't has a scanner and the host has Duo layout chrome. Skip-unless `duolayout` affordance stays. Gate stays `duo,capability:duo`. Do not overlay `always`, because unknown chrome-pass and a current iPhone must stay skipped-gate. Reinventing the app when it resizes, fixed widths and display-specific dependencies, and extreme layout changes as people fold scan from Apple's designing-for-iphone-duo page. Do not invent a fold pose or a vertical bar. Do not map designing-for-iphone-duo onto `ArrangementView`, `reservedRegions`, `data-id-*` markers, or a bare iPhone Duo phrase. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

92. CareKit advertising Don'ts. **Landed on this PR.** Account carekit Don'ts when every Don't has a scanner and the host has a care-plan widget. Skip-unless `carekit` affordance stays. Dedicated surface `carekit` attaches by appleUrl so ResearchKit keeps the cluster pack. Gate stays `capability:carekit`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. Advertising in a care plan scans from Apple's CareKit page. Do not invent a care plan or a permission sheet. Do not map carekit onto `import CareKit`, `import ResearchKit`, a HealthKit widget, `data-ck-ad`, or a bare CareKit phrase. Health permission stays human-only. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

93. ResearchKit critical-data Don'ts. **Landed on this PR.** Account researchkit Don'ts when every Don't has a scanner and the host has a study widget. Skip-unless `researchkit` affordance stays. Dedicated surface `researchkit` attaches by appleUrl before the health-research cluster so CareKit does not inherit the Don't. Gate stays `capability:researchkit`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. Requesting access to data that isn't critical to the study scans from Apple's ResearchKit page. Do not invent a study, a permission sheet, or a consent flow. Do not map researchkit onto `import ResearchKit`, `import CareKit`, a care-plan widget, `data-rk-critical`, or a bare ResearchKit phrase. Health permission and informed consent stay human-only. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

94. Wallet change-message Don'ts. **Landed on this PR.** Account wallet Don'ts when every Don't has a scanner and the host has a pass widget. Skip-unless `walletpass` affordance stays. Dedicated surface `wallet` attaches by appleUrl so Apple Pay does not inherit the Don't. Gate stays `capability:wallet`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. A change message used for marketing scans from Apple's Wallet page. Do not invent a pass or an Add to Wallet sheet. Do not map wallet onto `import PassKit`, `PKPass`, an Apple Pay button, `data-wl-marketing`, or a bare Wallet phrase. Decline-and-ask-again stays unscanned. Apple Pay stays capability-gated. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

95. App Clip Code Don'ts. **Landed on this PR.** Account app-clips Don'ts when every Don't has a scanner and the host has an App Clip Code widget. Skip-unless `appclipcode` affordance stays. Dedicated surface `app-clips` attaches by appleUrl before the platform cluster so Mac Catalyst does not inherit the Don't. Gate stays `capability:appclips`. Do not overlay `always`, because unknown chrome-pass must stay skipped-gate. A homemade or modified App Clip Code scans from Apple's App Clips page. Do not invent an App Clip Code. Do not map app-clips onto the App Clip entitlement, `data-app-clip`, `data-ac-modified`, or a bare App Clip phrase. Ads, web views, and account walls stay unscanned. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

96. App Clip Code presentation Don'ts. **Landed on this PR.** Account overlay, motion, and rotation when every Don't has a scanner and the host has the same App Clip Code widget. Skip-unless `appclipcode` stays. Gate stays `capability:appclips`. Do not overlay `always`. Text, logos, or images over the code, an animated or dimmed code, and a rotated code scan from Apple's App Clips page. Do not invent an App Clip Code. Do not map these Don'ts onto `data-ac-overlay`, `data-ac-motion`, `data-ac-rotate`, or the homemade-code scanner. Ads stay unscanned. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

97. App Clip Code aspect-ratio Don'ts. **Landed on this PR.** Account a changed aspect ratio when every Don't has a scanner and the host has the same App Clip Code widget. Skip-unless `appclipcode` stays. Gate stays `capability:appclips`. Do not overlay `always`. Scaling that changes the generated code's aspect ratio scans from Apple's App Clips page. Do not invent an App Clip Code. Do not map this Don't onto `data-ac-aspect`, letterbox padding, or a stretched photo. Ads stay unscanned. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

98. App Clip Code symbol Don'ts. **Landed on this PR.** Account a symbol added to the code when every Don't has a scanner and the host has the same App Clip Code widget. Skip-unless `appclipcode` stays. Gate stays `capability:appclips`. Do not overlay `always`. Adding a symbol to a generated App Clip Code scans from Apple's App Clips page. Do not invent an App Clip Code. Do not map this Don't onto `data-ac-symbol`, an image overlay, or an SF Symbol. Ads stay unscanned. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

99. Wallet decline Don'ts. **Landed on this PR.** Account a second suggestion after people decline when every Don't has a scanner and the host has the same pass widget. Skip-unless `walletpass` stays. Gate stays `capability:wallet`. Do not overlay `always`. If people decline your suggestion, don't ask them again, from Apple's Wallet page. Do not complete an Add to Wallet sheet. Do not map this Don't onto `data-wl-decline`, the first suggestion, a permission prompt, or Apple Pay. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

100. ShazamKit microphone Don'ts. **Landed on this PR.** Account a microphone that stays on after recognition when every Don't has a scanner and the host has a recognition session. Skip-unless `shazam` stays. Dedicated surface `shazamkit` attaches by appleUrl before the media cluster. Gate stays `capability:shazam`. Do not overlay `always`. People don't expect the microphone to stay on, from Apple's ShazamKit page. Do not complete a microphone permission sheet. Do not map this Don't onto `import ShazamKit`, `data-sz-mic`, an iCloud song-library opt-in, or packed privacy. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
101. Photo-editing Cancel Don'ts. **Landed on this PR.** Account Cancel that discards edits without a confirm when every Don't has a scanner and the host has an editing session. Skip-unless `photoedit` stays. Dedicated surface `photo-editing` attaches by appleUrl before the media cluster. Gate stays `capability:photos`. Do not overlay `always`. Ask people to confirm Cancel when edits exist, from Apple's photo-editing page. Do not invent a confirm dialog. Do not map this Don't onto `import PhotosUI`, `data-px-cancel`, an `<img>`, a custom top toolbar, or the allowed "edits will be lost" copy. The Mac menu bar stays an unpackaged stub. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
102. Wallet logo shadow Don'ts. **Landed on this PR.** Account inner drop shadows on logo artwork when every Don't has a scanner and the host has a pass. Same surface `wallet` and widget `data-wallet`. Gate stays `capability:wallet`. Do not overlay `always`. Avoid inner drop shadows on logo artwork, from Apple's Wallet page. Do not invent a logo. Do not map this Don't onto page chrome, image padding, strip-image text, or Apple Pay. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
103. Notification badge Don'ts. **Landed on this PR.** Account a badge that conveys numeric information that isn't related to notifications when every Don't has a scanner and the host sends notifications. Same surface `notifications`. An unread count stays allowed. `applicationIconBadgeNumber` is not the affordance. Do not invent a badge view. Do not map this Don't onto an action-button label. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
104. Notification action-label Don'ts. **Landed on this PR.** Account the app name in a notification button label when every Don't has a scanner and the host sends notifications. Same surface `notifications`. The display name must already be written in the host. A Snooze title stays allowed. Do not invent a bundle name or a notification action. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
105. Notification open-action Don'ts. **Landed on this PR.** Account an action that merely opens the app when every Don't has a scanner and the host sends notifications. Same surface `notifications`. A title that is exactly Open is the signal. A Snooze title stays allowed. Open Acme stays on the label scanner. Do not invent a notification action. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
106. Notification content-name Don'ts. **Landed on this PR.** Account the app name or icon inside the notification content when every Don't has a scanner and the host sends notifications. Same surface `notifications`. The display name must already be written in the host. Action-button titles stay on the label scanner. A New message title stays allowed. An attachment is not the app name. Do not invent a content title. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
107. Tracking pre-alert Don'ts. **Landed on this PR.** Account a custom tracking screen with an incentive or an Allow button when every Don't has a scanner. Same surface `privacy`. Continue and Next stay allowed. Camera prompts stay on the marketing scanner. Do not invent a tracking alert. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
108. Tracking leave Don'ts. **Landed on this PR.** Account a Close or Cancel button on a custom tracking screen when every Don't has a scanner. Same surface `privacy`. Continue and Next stay allowed. A legal-consent action stays allowed. An Allow button stays on the pre-alert scanner. Do not invent a tracking alert. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
109. Primary destructive Don'ts. **Landed on this PR.** Account the primary role on a button that performs a destructive action when every Don't has a scanner. Same surface `controls`. A plain Delete button stays allowed. A destructive role without the prominent style stays allowed. A prominent Save stays allowed. Do not restyle the button. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
110. Alert error-title Don'ts. **Landed on this PR.** Account an alert title that is only Error or an error number when every Don't has a scanner. Same surface `sheets`. A specific title stays allowed. A status toast stays on feedback. SwiftUI `.alert(` is not the overlay affordance. Do not rewrite the title. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
111. Alert cancel-default Don'ts. **Landed on this PR.** Account a Cancel button that is the default button in an alert when every Don't has a scanner. Same surface `sheets`. A plain Cancel button stays allowed. A primary Done button stays allowed. A Cancel button outside an alert stays skipped. Do not rewrite the label. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
112. Alert Yes/No Don'ts. **Landed on this PR.** Account a Yes or No button in an alert when every Don't has a scanner. Same surface `sheets`. A verb title stays allowed. A longer phrase such as No thanks stays allowed. A Yes button outside an alert stays skipped. Do not rewrite the label. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
113. Alert caution-symbol Don'ts. **Landed on this PR.** Account a caution symbol on a Save or Empty Trash alert when every Don't has a scanner. Same surface `sheets`. A different confirm stays allowed. A Save button without the symbol stays allowed. The symbol outside an alert stays skipped. Do not remove the symbol. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
114. Sheet trio Don'ts. **Landed on this PR.** Account Cancel, Done, and Back together in a sheet when every Don't has a scanner. Same surface `sheets`. A pair stays allowed. An alert controller stays allowed. The three buttons outside a sheet stay skipped. Do not remove a button. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
115. Sheet Done-only Don'ts. **Landed on this PR.** Account relying solely on the Done button to exit a sheet when every Don't has a scanner. Same surface `sheets`. A lone Done button in a dialog stays allowed, because a single-button alert uses Done. A `.sheet` whose only button is Done is the violation. Done paired with Cancel, Close, or Back stays allowed. The three-button trio stays on its own scanner. A Done action on an alert controller stays allowed. A Done button outside a sheet stays skipped. Do not invent Cancel. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
116. Tab-bar disabled Don'ts. **Landed on this PR.** Account a disabled or hidden tab bar button when every Don't has a scanner. Same surface `navigation`. A disabled button that is not a tab stays allowed. A tab view stays allowed. Do not enable the tab. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
120. Segment-count Don'ts. **Landed on this PR.** Account a segmented control with eight or more segments when every Don't has a scanner. Same surface `controls`. Seven or fewer stays allowed, because Apple allows about five to seven. Do not remove a segment. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
121. Spinner-label Don'ts. **Landed on this PR.** Account a label on a spinning progress indicator when every Don't has a scanner. Same surface `progress`. An `aria-label` is not visible text. A determinate bar that shows a percent stays allowed. A spinner with no label stays allowed. The Apple sentence without a spinner stays skipped. Do not remove the spinner. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
122. Submenu-available Don'ts. **Landed on this PR.** Account a submenu item that is unavailable when its nested items are unavailable when every Don't has a scanner. Same surface `menus`. A disabled command that does not open a submenu stays allowed. An enabled submenu stays allowed. A hidden menu item stays on the hide-unavailable scanner. The Apple sentence without a submenu stays skipped. Do not enable the submenu. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
123. Minimal-scrub Don'ts. **Landed on this PR.** Account a scrubber on a page control that uses the minimal background style when every Don't has a scanner. Same surface `page-controls`. A minimal page control without a scrubber stays allowed. A prominent page control with a scrubber stays allowed. The Apple sentence without a page control stays skipped. Do not remove the dots. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
134. Alert-title-lines Don'ts. **Landed on this PR.** Account an alert title that wraps to more than two lines when every Don't has a scanner. Same surface `sheets`. A one-line or two-line title stays allowed. A long message under a short title stays allowed. A sheet title stays allowed. The Apple sentence without an alert stays skipped. Do not shorten the title. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
133. Toolbar-unnamed Don'ts. **Landed on this PR.** Account a toolbar item with no visible text and no accessible name when every Don't has a scanner. Same surface `navigation`. A symbol with an accessible name stays allowed. A text button stays allowed. An unnamed button outside a toolbar stays allowed. The Apple sentence without a toolbar in that file stays allowed. Do not invent a label. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
132. Window-app-title Don'ts. **Landed on this PR.** Account a window titled with the app name when every Don't has a scanner. Same surface `navigation`. A content title stays allowed. A document title stays allowed. `UIWindow` stays allowed. The Apple sentence without an app window in that file stays allowed. Do not rewrite the title. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
131. Sidebar-depth Don'ts. **Landed on this PR.** Account a third level of hierarchy in a sidebar when every Don't has a scanner. Same surface `navigation`. Two levels stay allowed. A nested list outside a sidebar stays allowed. Sections in the detail column stay allowed. The Apple sentence without a sidebar in that file stays allowed. Do not flatten the list. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
130. Column-punctuation Don'ts. **Landed on this PR.** Account a column heading that ends with punctuation when every Don't has a scanner. Same surface `lists-split`. A heading without punctuation stays allowed. A single-column table stays allowed. An outline colon stays on the outline scanner. The Apple sentence without a multicolumn table stays allowed. Do not rewrite the heading. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
129. Context-shortcut Don'ts. **Landed on this PR.** Account a keyboard shortcut inside a context menu when every Don't has a scanner. Same surface `menus`. A shortcut on a main menu stays allowed. A shortcut on a button stays allowed. The Apple sentence without a context menu stays skipped. Do not remove the shortcut. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
128. Color-only toggle Don'ts. **Landed on this PR.** Account a toggle that relies solely on different colors to communicate state when every Don't has a scanner. Same surface `controls`. An icon, a checkmark, or a labeled switch stays allowed. The Apple sentence without that control stays allowed. Do not recolor the control. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
127. Selection-label Don'ts. **Landed on this PR.** Account a label that explains a button that changes the selection when every Don't has a scanner. Same surface `controls`. The signal is `changesSelectionAsPrimaryAction`. An image-only button stays allowed. A labeled switch stays allowed. The Apple sentence without that button stays allowed. Do not remove the label. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
126. Scrub-animation Don'ts. **Landed on this PR.** Account an animated page transition while a page control is scrubbing when every Don't has a scanner. Same surface `page-controls`. A scrubber without that animation stays allowed. An animated tap stays allowed. The Apple sentence without a page control stays skipped. Do not remove the dots. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
125. Scroll-indicator Don'ts. **Landed on this PR.** Account a scrolling indicator on a scroll view that also shows a page control when every Don't has a scanner. Same surface `scroll-views`. A scroll view without a page control stays allowed. A page control without a scroll view stays skipped. A hidden indicator stays allowed. Document overflow is not a scroll view. Do not hide the indicator. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
124. Segment-role Don'ts. **Landed on this PR.** Account a segmented control that both selects and acts when every Don't has a scanner. Same surface `controls`. A radio-only control stays allowed. An action row with no selection state stays allowed. The Apple sentence without a segmented control stays allowed. Do not restyle a segment. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
119. Index-beside-disclosure Don'ts. **Landed on this PR.** Account a section index on a list that also shows disclosure indicators when every Don't has a scanner. Same surface `lists-split`. Either widget alone stays allowed. The Apple sentence without a list stays allowed. Do not remove the index. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
118. Segment-mix Don'ts. **Landed on this PR.** Account a segmented control that mixes text and images when every Don't has a scanner. Same surface `controls`. An image-only control stays allowed. A text-only control stays allowed. Every segment carrying both a word and an image stays allowed. An `aria-label` is not visible text. A text segment beside an image-only segment stays pending. Do not restyle a segment. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.
117. Too-many-radios Don'ts. **Landed on this PR.** Account a set of more than about five radio buttons when every Don't has a scanner. Same surface `controls`. A set of five or fewer stays allowed. A `role="radio"` view switcher stays allowed. Six radios that share a name stay pending. A SwiftUI `Picker` with `.pickerStyle(.radioGroup)` and more than five tags stays pending. The Apple sentence without a radio stays allowed. Do not replace radios with a pop-up. Do not scan segmented controls, action sheets, or a Help button. Design-principles Don'ts stay pending. Visual any-host Apple-ness is still unproven.

## Non-goals

- A widget kit
- TypeSafe at `/hig` apply time
- Failing backends for having no UI
- Authoring Watch/TV/Vision apply surfaces in the first catalog PR
- Merging PR #12 unless asked
- Touching `raashishah.github.io` or Dreamachine

## Verify (this PR)

```bash
node eval/run-dry.mjs
node eval/run-chrome-grammar.mjs
node eval/run-swarm.mjs
node eval/run-check-chrome.mjs
node eval/run-catalog.mjs
```

`run-swarm.mjs` still requires `requiredIds.length === 12` and Duo off that list. Dry eval: no `appleTypeDefault` / `type_default=apple`. `run-catalog.mjs`: live-index diff with no expected integer; list host applicable for Lists; web host not applicable for complications; fail if a topic `passWhen` names a framework or a font family; wave 0 then catalog; remaining 0 is done; brand app-shell rows `n/a-register`; contract does not stop at twelve; missing host widgets (required patterns and optional ) are `skipped-no-affordance`; `apply-catalog.mjs` accounts chrome-backed packed topics, pack Don't code spans, and required-surface prose Don'ts on tmp hosts without freezing remaining as an integer. `run-check-chrome.mjs`: same P0 FAILs on `chrome-antipatterns-web` and `chrome-antipatterns-swift`; `apply-chrome.mjs --write` on tmp copies of those hosts and of `chrome-antipatterns` clears owned P0s and is a no-op on `chrome-pass`; committed fixtures stay dirty.
