# Chrome anti-pattern fixture (eval only)

Minimal surfaces that encode Pink Depot-class structure failures plus glass-unsafe opaque bar fill.
Not a real app. Brand tokens intentionally absent.
Agents and `run-chrome-grammar.mjs` use `manifest.yaml` + `expected.json`.
