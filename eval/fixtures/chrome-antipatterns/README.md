# Chrome anti-pattern fixture (eval only)

Minimal surfaces that encode Pink Depot-class structure failures plus glass-unsafe opaque bar fill, fashion glass, card-grid home, and nested cards.
Not a real app. Brand tokens intentionally absent.
Agents, `run-chrome-grammar.mjs`, and `check-chrome.mjs` use `manifest.yaml` + `expected.json`.

