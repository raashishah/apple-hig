# Chrome anti-pattern Swift host (eval only)

Swift source that encodes the same design FAILs as `chrome-antipatterns-web`.
Grammar `failWhen` / `passWhen` stay framework-free; detectors map the host widgets.
Not a real app.
