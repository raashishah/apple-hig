# `/hig` portable done checklist

Proof patient: `admissions-app` (admissionsdemo).  
Dogfood product: Pink Depot (see `docs/PROOF.md`).

Do **not** auto-mutate finished designed apps unless the user asks to dogfood.

## Admissionsdemo (live proof)

- [x] `~/.cursor/skills/hig` exists and points at plugin skill root
- [x] Bare `/hig` produced `DESIGN.md` (non-placeholder, Apple-designer prose for this product)
- [x] `.hig/app-design.md` exists (nav model, screens, states)
- [x] `.hig/screens.yaml` lists primary routes/screens
- [x] Same run implemented structure/chrome on primary screens
- [x] Brand tokens/fonts for that app unchanged
- [x] Evidence screenshots @768 and @375 under `.hig/evidence/`
- [x] Product register (not marketing landing chrome)

## Dry harness (no designed-app mutation)

From repo root:

```bash
node eval/run-dry.mjs
node eval/run-chrome-grammar.mjs
node eval/run-swarm.mjs
node eval/run-check-chrome.mjs
node eval/run-catalog.mjs
```

- [x] Sparse fixture: preflight passes; mutation open
- [x] Unsupported fixture: `mutation=unsupported` with one-line stop
- [x] Brand veto: `register: brand` → `review_adapt_mutation=blocked`
- [x] Grammar loads; required chrome rule IDs present (including filter-density + fashion-glass + card-grid-home)
- [x] `fixtures/chrome-antipatterns` covers view-mode, toolbar budget, filter density, form column, sidebar, opaque bars, fashion glass, card-grid home, nested cards
- [x] `check-chrome.mjs` detects those antipatterns and stays quiet on `fixtures/chrome-pass`
- [x] Host fonts stay (no forced SF Pro / system stack)
- [x] `surfaces.yaml` apply SSOT; `requiredIds` stay 12; Duo stays gated
- [x] Packs + SKILL/review/design wire chrome grammar IDs
- [x] All chrome rules are `mutationClass: structure` (brand veto does not hide them)
- [x] `catalog.yaml` tracks the live Apple article index; lists apply on Vue; complications skip on web; `requiredIds` stay 12
- [x] Goal loop: wave 0 is `requiredIds`; after chrome P0 remaining packed catalog topics apply; done when `remaining` is 0; brand app-shell catalog rows are `n/a-register`
- [x] Host-pattern affordance: list/form/overlay/chrome skip when the widget is absent; collection/card grids still count as list
- [x] Optional widget affordance: menu/picker/progress/search/notification/loading/feedback/onboarding/drag skip when absent; Search nav link is not search; `<select>` is picker not menu; remaining is not a frozen integer; `requiredIds` stay 12
- [x] Settings/undo skip-unless: chrome-pass skips both; a Settings screen or Undo control stays pending; a data-entry form is not Settings; Cancel is not Undo; remaining is not a frozen integer
- [x] Search Don't heuristics account search-fields/searching on tmp chrome-pass (already-compliant); spinner-on-keystroke applies; hide-only-path and command-dump stay pending without inventing a list; remaining is not a frozen integer
- [x] Writing Don't heuristics account writing on tmp chrome-pass (already-compliant); Title Case long help sentence-cases; sarcastic errors without a next step and rewritten system alerts stay pending without inventing copy; remaining is not a frozen integer
- [x] Privacy Don't heuristics account privacy on tmp chrome-pass (already-compliant); hidden Don't Allow unhides; Allow-only and marketing camera prompts stay pending without inventing a decline; remaining is not a frozen integer
- [x] Branding Don't heuristics account branding on tmp chrome-pass (already-compliant); watermarks strip; mixed outlined/system toolbars stay pending without injecting a kit; other foundations stay pending; remaining is not a frozen integer
- [x] Icons/images/app-icons/inclusion Don't heuristics account those topics on tmp chrome-pass (already-compliant); decorative heading icons, screenshot empty-states, and app-icon masks strip; outlined doodles and ability jokes stay pending without injecting a kit or inventing copy; design-principles stay pending; remaining is not a frozen integer
- [x] Optional-widget Don't heuristics account settings/undo/loading/feedback/onboarding/drag/notifications/launching when the host has that widget and every Don't has a scanner; chrome-pass still skips missing widgets; clean Settings/Undo are already-compliant; re-theme fills, CRUD confetti, and launch autoplay strip; unfixable walls stay pending; design-principles stay pending; remaining is not a frozen integer
- [x] Chrome-backed Don't heuristics account layout/materials/lists/forms/navigation when every Don't has a scanner; chrome-pass those topics are already-compliant; list min-widths, equal-weight submits, list-toolbar CTAs, marketing tab shells, stacked glass, compatibility flags, relative bottom nav, and card-grid masters apply; packs with Don't bullets account through Don't scanners, not clean chromeIds; design-principles stay pending; remaining is not a frozen integer
- [x] Host-widget Don't heuristics account menus/pickers/progress/controls when every Don't has a scanner; chrome-pass still skips missing menus/pickers/progress; buttons stay already-compliant; hidden menu items, overweight wheels, progress morph/jump/pull titles, OK+save, and submit-toggles apply; nested submenus stay pending without inventing a flatter menu; design-principles stay pending; remaining is not a frozen integer
- [x] System-chrome Don't heuristics account widgets/Live Activities/status bars/Control Center when every Don't has a scanner and the host has that capability; chrome-pass still skips those gates; clean capability hosts are already-compliant; fake widgets, stretched widgets, Island pointers, Live Activity ads, hidden status bars, fake clocks, opaque status strips, and settings-row Control Center chrome apply; app-icon widgets and one-symbol toggles stay pending; design-principles stay pending; remaining is not a frozen integer
- [x] RTL Don't heuristics account right-to-left when every Don't has a scanner; chrome-pass is already-compliant; whole-window `scaleX(-1)`, physical `margin-left`/`padding-right`, `dir="auto"` roots, and `chevron-left`/`chevron.left` back controls apply to logical; Unicode `←` back chevrons stay pending; design-principles stay pending; remaining is not a frozen integer
- [x] Nested-modal Don't heuristics account sheets/alerts/action-sheets/modality when every Don't has a scanner; chrome-pass still skips missing overlays; `data-nested-modal` strips; nested `<dialog>` stacks stay pending without inventing a sheet; design-principles stay pending; remaining is not a frozen integer
- [x] Composed `also:` URLs pack dark-mode, SF Symbols, context/pull-down/pop-up menus, and notifications glanceability onto existing complete Don't packs; chrome-pass dark-mode/SF Symbols are already-compliant; missing menus/notifications stay skipped-no-affordance; tab-views skip-unless a tabbed pane; the Mac menu bar stays an unpackaged stub; no second swarm lease; remaining is not a frozen integer
- [x] Slider, scroll-view, and popover Don't heuristics account those topics when every Don't has a scanner and the host has that widget; chrome-pass still skips missing sliders/scroll panes/popovers; volume/nested-scroll/popover markers strip; a live volume slider, nested overflow panes, and nested popovers stay pending without inventing a volume view, flattening scroll, or turning a popover into a sheet; `type=range` is not a picker; document/body overflow is not a scroll view; a sheet `dialog` is not a popover; design-principles stay pending; remaining is not a frozen integer
- [x] Collection Don't heuristics account collections when every Don't has a scanner and the host has that widget; chrome-pass still skips missing collections; a `<ul>` inventory stays a list; custom-layout / text-collection / overlap markers strip on a visual collection with an image; a text-only collection of spans stays pending without inventing a table or flattening into a list; design-principles stay pending; remaining is not a frozen integer
- [x] Page-control Don't heuristics account page-controls when every Don't has a scanner and the host has that widget; chrome-pass still skips missing page controls; a progress bar and numbered pagination stay themselves; hierarchy / too-many-dots / extra-indicator-image / colored-indicator markers strip; a live `numberOfPages={12}` page control stays pending without inventing a grid; design-principles stay pending; remaining is not a frozen integer
- [x] Label Don't heuristics account labels when every Don't has a scanner and the host has a static label widget; chrome-pass still skips missing static labels; a form `<label>` and `aria-label` stay themselves; editable / long / unselectable markers strip; a live `contenteditable` label stays pending without inventing a text field; host fonts stay; design-principles stay pending; remaining is not a frozen integer
- [x] Text-view Don't heuristics account text-views when every Don't has a scanner and the host has a text-view widget; chrome-pass still skips missing text views; an `<input>` stays a field; short / unselectable markers strip; a live 1-row textarea stays pending without inventing a label; host fonts stay; design-principles stay pending; remaining is not a frozen integer
- [x] Image-view Don't heuristics account image-views when every Don't has a scanner and the host has an image-view widget; chrome-pass still skips missing image views; every `<img>` stays itself; interactive / icon / overlay markers strip; a live clickable image view stays pending without inventing a button; design-principles stay pending; remaining is not a frozen integer
- [x] Chart Don't heuristics account charts and charting-data when every Don't has a scanner and the host has a chart widget; chrome-pass still skips missing charts; a table and a decorative SVG stay themselves; color-only / hover-only / as-table / overcrowded markers strip; a live hover-only chart stays pending without inventing a table; design-principles stay pending; remaining is not a frozen integer
- [x] Disclosure-control Don't heuristics account disclosure-controls when every Don't has a scanner and the host has a disclosure widget; chrome-pass still skips missing disclosure widgets; an `aria-expanded` toolbar stays itself; extra-button / unlabeled / unhidden markers strip; a live unlabeled `<details>` stays pending without inventing a summary; design-principles stay pending; remaining is not a frozen integer
- [x] Box Don't heuristics account boxes when every Don't has a scanner and the host has a dedicated box widget; chrome-pass still skips missing boxes; a card, grouped list, and form fieldset stay themselves; nested / oversized markers strip; live nested `data-box` stays pending without un-nesting; design-principles stay pending; remaining is not a frozen integer
- [x] Edit-menu Don't heuristics account edit-menus when every Don't has a scanner and the host has a real edit menu; chrome-pass still skips missing edit menus; a command `role="menu"` stays a menu; custom / no-selection / redundant markers strip; live Cut/Copy on `data-edit-menu` without a selection stays pending without inventing dimming; design-principles stay pending; remaining is not a frozen integer
- [x] Offering-help Don't heuristics account offering-help when every Don't has a scanner and the host has a tip, tooltip, or help overlay; chrome-pass still skips missing help widgets; onboarding and a `title=` attribute stay themselves; wrong-platform / standard-component / promotional markers strip; live "click on iPhone" help copy stays pending without inventing platform verbs; design-principles stay pending; remaining is not a frozen integer
- [x] Web-view Don't heuristics account web-views when every Don't has a scanner and the host has an embedded web view; chrome-pass still skips missing web views; the host document stays itself; missing-back-forward / Safari-replica markers strip; a live multi-page `iframe` without Back/Forward stays pending without inventing chrome; design-principles stay pending; remaining is not a frozen integer
- [x] Activity-view Don't heuristics account activity-views when every Don't has a scanner and the host has a share sheet; chrome-pass still skips missing share sheets; a Share nav link stays itself; duplicate-action / alternative-reveal markers strip; live duplicate Print rows stay pending without inventing a custom title; design-principles stay pending; remaining is not a frozen integer
- [x] Printing Don't heuristics account printing when every Don't has a scanner and the host has a dedicated Print action; chrome-pass still skips missing Print actions; a share-sheet Print row stays itself; print-nothing / page-orientation markers strip; live Print on `data-nothing-printable` stays pending without inventing dimming; design-principles stay pending; remaining is not a frozen integer
- [x] Going-full-screen Don't heuristics account going-full-screen when every Don't has a scanner and the host has a dedicated full-screen experience; chrome-pass still skips missing full-screen experiences; `100vh` and a video's native controls stay themselves; programmatic-resize / auto-exit / window-mode-menu markers strip; live `resizeTo` on `data-fullscreen` stays pending without inventing layout; design-principles stay pending; remaining is not a frozen integer
- [x] File-management Don't heuristics account file-management when every Don't has a scanner and the host has a document browser; chrome-pass still skips missing document browsers; `<input type="file">` stays itself; custom-toolbar / extensions-shown / explicit-save markers strip; live Save on `data-no-autosave` stays pending without inventing autosave; design-principles stay pending; remaining is not a frozen integer
- [x] Focus-and-selection Don't heuristics account focus-and-selection when every Don't has a scanner and the host has a dedicated focus system; chrome-pass still skips missing focus systems; `:focus` CSS, `tabindex`, and `autofocus` alone stay themselves; steal-focus / custom-effect markers strip; live `.focus()` on `data-focus-system` stays pending without inventing a ring; design-principles stay pending; remaining is not a frozen integer
- [x] Managing-accounts Don't heuristics account managing-accounts when every Don't has a scanner and the host has dedicated account or sign-in chrome; chrome-pass still skips missing account screens; a form, Settings, a password field, and Sign in with Apple stay themselves; force-account / buried-deletion / passcode-auth markers strip; live `data-no-guest` plus passcode copy stays pending without inventing a guest path, deletion flow, or Face ID; design-principles stay pending; remaining is not a frozen integer
- [x] Tab-view Don't heuristics account tab-views when every Don't has a scanner and the host has an NSTabView-style tabbed pane; chrome-pass still skips missing tabbed panes; a bottom tab bar stays itself; popup / too-many / cross-pane markers strip; live seven tabs stay pending without inventing a menu; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Multitasking Don't heuristics account multitasking when every Don't has a scanner and the host has dedicated multitasking or Picture in Picture chrome; chrome-pass still skips missing sessions; a video element stays itself; pause-on-switch / notify-routine / audio-interrupt markers strip; live `data-keep-playing` stays pending without inventing `visibilitychange`, `.pause()`, or a notification; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Ratings-and-reviews Don't heuristics account ratings-and-reviews when every Don't has a scanner and the host has a ratings prompt; chrome-pass still skips missing prompts; a star glyph and onboarding stay themselves; first-launch / interrupt / pester markers strip; live double `requestReview()` stays pending without inventing StoreKit or a delay; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Windows Don't heuristics account windows when every Don't has a scanner and the host has a dedicated app window; chrome-pass still skips missing windows; the host document and `UIWindow` stay themselves; default-open / custom-frame / call-scene / critical-bottom-bar markers strip; live `data-window` footer Save stays pending without inventing an inspector; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Playing-video Don't heuristics account playing-video when every Don't has a scanner and the host has a video player; chrome-pass still skips missing players; `100vh` and Picture in Picture chrome stay themselves; custom-player / letterbox / resume-prompt / loading-splash markers strip; live `data-video-player` Resume playback? stays pending without inventing autoplay or a Space handler; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Playing-haptics Don't heuristics account playing-haptics when every Don't has a scanner and the host has a haptic API; chrome-pass still skips missing haptics; a button tap stays itself; wrong-meaning / overuse / required markers strip; live `navigator.vibrate` on `data-haptic` stays pending without inventing a mute toggle; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] AirPlay Don't heuristics account airplay when every Don't has a scanner and the host has AirPlay routing; chrome-pass still skips missing AirPlay; every `<video>` and a Share control stay themselves; stop-on-background / interrupt / auto-mirror / background-loop markers strip; live `data-airplay` plus autoplay video stays pending without inventing a route picker; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Gyro-and-accelerometer Don't heuristics account gyro-and-accelerometer when every Don't has a scanner and the host has a motion API; chrome-pass still skips missing gyros; a swipe and CSS motion stay themselves; no-benefit / direct-ui markers strip; live `data-gyro` plus `devicemotion` stays pending without inventing Core Motion; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Home Screen quick-action Don't heuristics account home-screen-quick-actions when every Don't has a scanner and the host has that widget; chrome-pass still skips missing quick actions; a context menu and Share stay themselves; app-name / emoji markers strip; live `data-quick-action` plus an emoji title stays pending without inventing an SF Symbol; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Live-viewing Don't heuristics account live-viewing-apps when every Don't has a scanner and the host has dedicated live-viewing chrome; chrome-pass still skips missing live-viewing chrome; every `<video>` stays itself; unmarked / audio-after-leave markers strip; live `data-live-viewing` plus On demand copy stays pending without inventing a live badge; packed playing-video and packed multitasking stay themselves; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Snippet Don't heuristics account snippets when every Don't has a scanner and the host has a Siri or App Shortcut snippet; chrome-pass still skips missing snippets; a card and packed App Shortcuts stay themselves; dialogue / too-tall markers strip; live `data-snippet` plus height 480 stays pending without inventing a max-height; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Generative-AI Don't heuristics account generative-ai when every Don't has a scanner and the host has a generative feature; chrome-pass still skips missing generative features; a textarea and packed inclusion stay themselves; as-human / no-revert markers strip; live `data-generative` plus a human-authored claim stays pending without inventing disclosure copy, Undo, or Retry; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Always-On Don't heuristics account always-on when every Don't has a scanner and the host has Always On chrome; chrome-pass still skips missing Always On; dimmed CSS and packed Live Activities stay themselves; sensitive / stop-motion markers strip; live `data-always-on` plus a bank balance stays pending without inventing redaction; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] SharePlay Don't heuristics account shareplay when every Don't has a scanner and the host has SharePlay chrome; chrome-pass still skips missing SharePlay; a Share control and packed activity-views stay themselves; adjective / inflected markers strip; live `data-shareplay` plus Spatial SharePlay stays pending without inventing GroupActivity; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Nearby-interaction Don't heuristics account nearby-interactions when every Don't has a scanner and the host has Nearby Interaction chrome; chrome-pass still skips missing Nearby Interaction; a Share control, NFC, and geolocation stay themselves; only-way / portrait markers strip; live `data-nearby` plus the only way to transfer stays pending without inventing NISession; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Activity-rings Don't heuristics account activity-rings when every Don't has a scanner and the host has an Activity ring widget; packed `gate: always` overlays the watch slug; chrome-pass still skips missing rings; a progress bar and a CSS circle stay themselves; other-data / multi-person / recolor / decoration markers strip; live `data-activity-rings` plus other types of data stays pending without inventing HKActivityRingView; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] NFC Don't heuristics account nfc when every Don't has a scanner and the host has NFC chrome; packed `gate: always` overlays catalog `capability:nfc`; chrome-pass still skips missing NFC; Nearby Interaction, Wallet, Tap to Pay, and packed Apple Pay stay themselves; contact / jargon markers strip; live `data-nfc` plus tap-your-phone / NFC tag copy stays pending without inventing NFCNDEFReaderSession; do not complete an NFC read; the commerce cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Augmented-reality Don't heuristics account augmented-reality when every Don't has a scanner and the host has AR chrome; packed `gate: always` overlays catalog `capability:arkit`; chrome-pass still skips missing AR; every image, CSS 3D, Nearby Interaction, and NFC stay themselves; jargon / glyph markers strip; live `data-ar` plus Adjust tracking stays pending without inventing ARView; do not invent ARKit, a reset, or a coaching view; the media-intelligence cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Tap to Pay Don't heuristics account tap-to-pay-on-iphone when every Don't has a scanner and the host has Tap to Pay chrome; packed `gate: always` overlays catalog `capability:taptopay`; chrome-pass still skips missing Tap to Pay; NFC, Wallet, and packed Apple Pay stay themselves; Apple-logo / nonpayment-label markers strip; live `data-tap-to-pay` plus Look Up / Tap to Pay on iPhone stays pending without inventing ProximityReader; do not invent Tap to Pay or complete a payment; the commerce cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] ID Verifier Don't heuristics account id-verifier when every Don't has a scanner and the host has ID Verifier chrome; packed `gate: always` overlays catalog `capability:idverifier`; chrome-pass still skips missing ID Verifier; NFC, Tap to Pay, Wallet, and packed Apple Pay stay themselves; Apple-logo / communication-symbol markers strip; live `data-id-verifier` plus NFC / Verify Age stays pending without inventing MobileDriversLicenseDisplayRequest; do not invent ID Verifier or complete a verification; the commerce cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] In-app purchase Don't heuristics account apple-in-app-purchase when every Don't has a scanner and the host has IAP chrome; packed `gate: always` overlays catalog `capability:iap`; chrome-pass still skips missing IAP; packed Apple Pay, Tap to Pay, Wallet, and ID Verifier stay themselves; confirmation-sheet / refund-buried / refund-policy markers strip; live `data-in-app-purchase` plus refund-policy copy stays pending without inventing StoreKit; do not invent IAP or complete a purchase; the commerce cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Maps Don't heuristics account maps when every Don't has a scanner and the host has MapKit chrome; packed `gate: always` overlays catalog `capability:mapkit`; chrome-pass still skips missing MapKit; packed CarPlay, a generic map word, and an iframe stay themselves; cover-legal / replica markers strip; live `data-map` plus covered-legal-link copy stays pending without inventing MKMapView; do not invent a map; the carplay-maps cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] HomeKit Don't heuristics account homekit when every Don't has a scanner and the host has HomeKit chrome; packed `gate: always` overlays catalog `capability:homekit`; chrome-pass still skips missing HomeKit; packed iCloud, a generic Home app word, and packed managing-accounts stay themselves; company-name / overwrite / duplicate-settings / cover-camera markers strip; live `data-homekit` plus suggested Siri service-name copy stays pending without inventing HMHomeManager; do not invent HomeKit; do not pack require-account; the icloud-shareplay cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Workouts Don't heuristics account workouts when every Don't has a scanner and the host has workout chrome; packed `gate: always` overlays catalog `always`; chrome-pass still skips missing workout chrome; packed Activity rings, packed HealthKit, and a progress bar stay themselves; distract / brief-session markers strip; live `data-workout` plus list-of-workouts-during-active-session copy stays pending without inventing HKWorkoutSession; do not invent a workout session; do not pack the Activity-rings bullet; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Live Photos Don't heuristics account live-photos when every Don't has a scanner and the host has Live Photo chrome; packed `gate: always` overlays the cluster appliesWhen; chrome-pass still skips missing Live Photo chrome; every still photo, every `<video>`, packed playing-video, and a bare Live Photo phrase stay themselves; disassemble / playback-button / unsupported-replica markers strip; live `data-live-photo` plus a video playback button stays pending without inventing PHLivePhotoView; do not invent a Live Photo; the media-intelligence cluster stays a live-link cluster; dedicated live-photos attaches by appleUrl; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] iCloud Don't heuristics account icloud when every Don't has a scanner and the host has iCloud chrome; packed `gate: always` overlays catalog `capability:icloud`; chrome-pass still skips missing iCloud chrome; packed file-management, packed SharePlay, and a bare iCloud phrase stay themselves; ask-docs / unavailable-alert / app-resources markers strip; live `data-icloud` plus which-documents-to-keep copy stays pending without inventing CKContainer; do not invent iCloud; the icloud-shareplay cluster stays a live-link cluster; dedicated icloud attaches by appleUrl; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Siri Don't heuristics account siri when every Don't has a scanner and the host has Siri chrome; packed `gate: always` overlays catalog `capability:siri`; chrome-pass still skips missing Siri chrome; packed App Shortcuts, packed snippets, packed inclusion, and a bare Siri / Hey Siri phrase stay themselves; advertise / impersonate / pronoun markers strip; live `data-siri` plus pronoun copy stays pending without inventing INInteraction; do not invent Siri or complete Siri permission; the siri-app-shortcuts cluster keeps app-shortcuts via `also:`; dedicated siri attaches by appleUrl; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] App Shortcuts leftover Don't heuristics account app-shortcuts when every Don't has a scanner and the host has App Shortcuts chrome; packed `gate: always` overlays catalog `capability:siri` on the existing cluster (no second exclusive `app-shortcuts` surface); chrome-pass still skips missing App Shortcuts chrome; packed Siri, packed snippets, and a bare App Shortcuts phrase stay themselves; reskin / lowercase / title-item markers strip; live `data-app-shortcuts` plus lowercase copy stays pending without inventing AppShortcutsProvider; do not invent App Intents; dedicated siri stays itself; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] HealthKit leftover Don't heuristics account healthkit when every Don't has a scanner and the host has HealthKit chrome; packed `gate: always` overlays catalog `capability:healthkit`; chrome-pass still skips missing HealthKit chrome; packed HomeKit, packed Activity rings, packed workouts, packed privacy, and a bare HealthKit phrase stay themselves; replica / sharing / term markers strip; live `data-healthkit` plus HealthKit copy stays pending without inventing HKHealthStore; do not invent HealthKit or complete Health permission; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] CarPlay leftover Don't heuristics account carplay when every Don't has a scanner and the host has CarPlay chrome; packed `gate: always` overlays catalog `capability:carplay,capability:mapkit`; chrome-pass still skips missing CarPlay chrome; packed maps and a bare CarPlay phrase stay themselves; iphone-lock / iphone-error / iphone-interact markers strip; live `data-carplay` plus unlock-iPhone copy stays pending without inventing CPInterfaceController; do not invent CarPlay or complete vehicle pairing; dedicated carplay attaches by appleUrl; the carplay-maps cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Apple Pay leftover Don't heuristics account apple-pay when every Don't has a scanner and the host has Apple Pay chrome; gate stays `capability:applepay` so a PassKit wallet host does not launch apple-pay; chrome-pass still skips the capability gate; packed Wallet, packed Tap to Pay, packed NFC, packed In-App Purchase, and a bare Apple Pay phrase stay themselves; mark / plural / logo markers strip; live `data-apple-pay` plus Apple Pays copy stays pending without inventing PKPaymentButton; do not invent Apple Pay or complete the payment sheet; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Sign in with Apple leftover Don't heuristics account sign-in-with-apple when every Don't has a scanner and the host has Sign in with Apple chrome; packed `gate: always` overlays catalog `capability:signinwithapple`; chrome-pass still skips missing Sign in with Apple chrome; packed managing-accounts, packed Apple Pay, and a bare Sign in with Apple phrase stay themselves; password / email / logo markers strip; live `data-siwa` plus create-a-password copy stays pending without inventing SignInWithAppleButton; do not invent Sign in with Apple or complete the system sign-in sheet; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Playing audio Don't heuristics account playing-audio when every Don't has a scanner and the host has playing-audio chrome; gate is `always`; chrome-pass still skips missing playing-audio chrome; a volume slider, a video element, packed AirPlay, and a bare audio phrase stay themselves; output-volume / repurpose / headphones markers strip; live `data-playing-audio` plus system-output-volume copy stays pending without inventing AVAudioSession; do not invent a player; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Game Center leftover Don't heuristics account game-center when every Don't has a scanner and the host has Game Center chrome; gate stays `capability:gamecenter`; chrome-pass still skips the capability gate; a bare Game Center phrase and import GameKit stay themselves; gameplay / artwork / terms markers strip; live `data-game-center` plus access-point-during-gameplay copy stays pending without inventing GKAccessPoint; do not invent Game Center or complete sign-in; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Panels Don't heuristics account panels when every Don't has a scanner and the host has panel chrome; gate is `always` and overlays catalog `desktop`; chrome-pass still skips missing panel chrome; a sheet, a regular window, `data-window`, and `NSOpenPanel` stay themselves; window-menu / minimize / HUD markers strip; live `data-panel` plus Window-menu-documents-list copy stays pending without inventing NSPanel; do not invent a panel; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Path controls Don't heuristics account path-controls when every Don't has a scanner and the host has path-control chrome; gate is `always` and overlays catalog `desktop`; chrome-pass still skips missing path-control chrome; a file browser, the iOS status bar, and a bare path word stay themselves; the toolbar marker strips; live `data-path-control` plus toolbar-or-status-bar copy stays pending without inventing NSPathControl; do not invent a path bar; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Outline views Don't heuristics account outline-views when every Don't has a scanner and the host has outline chrome; gate is `always` and overlays catalog `desktop`; chrome-pass still skips missing outline chrome; a plain list, a disclosure control, and a bare outline word stay themselves; colon / headings markers strip; live `data-outline` plus trailing-colon copy stays pending without inventing NSOutlineView; do not invent an outline; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] iMessage sticker Don't heuristics account imessage-apps-and-stickers when every Don't has a scanner and the host has sticker-pack chrome; gate is `always` and overlays catalog `capability:imessage`; chrome-pass still skips missing sticker-pack chrome; an image view, a plain list, a bare sticker phrase, and `MSStickerSize` stay themselves; the mixed-sizes marker strips; live `data-sticker-pack` plus mix-sizes copy stays pending without inventing MSSticker; do not invent a sticker pack or complete Messages; a file-size limit stays unscanned; the platform cluster stays a live-link cluster; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Action button Don't heuristics account action-button when every Don't has a scanner and the host has Action button chrome; gate is `always` and overlays catalog `phone`; chrome-pass still skips missing Action button chrome; an App Shortcut, a Home Screen quick action, a bare Action button phrase, and `data-ab-label` alone stay themselves; long-label / settings markers strip; a live `data-action-button` label longer than three words stays pending without inventing AppShortcutsProvider; do not invent an Action button; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Camera Control Don't heuristics account camera-control when every Don't has a scanner and the host has Camera Control chrome; gate is `always` and overlays catalog `phone`; chrome-pass still skips missing Camera Control chrome; a slider, `AVCaptureSession`, and a bare Camera Control phrase stay themselves; the duplicate marker strips; live `data-camera-control` plus duplicating-controls copy stays pending without inventing AVCaptureControl; do not invent a Camera Control; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Dock menu Don't heuristics account dock-menus when every Don't has a scanner and the host has Dock menu chrome; gate is `always` and overlays catalog `desktop`; chrome-pass still skips missing Dock menu chrome; a menu, a Home Screen quick action, and a bare Dock phrase stay themselves; the elsewhere marker strips; a live `data-dock-menu` item whose title appears only there stays pending without inventing applicationDockMenu; do not invent a Dock menu or a second command; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Gesture Don't heuristics account gestures when every Don't has a scanner and the host has gesture chrome; gate stays `phone,ipad`; unknown chrome-pass stays skipped-gate; a phone host with no gesture chrome is skipped-no-affordance; a button, a tap phrase, `MagnifyGesture`, and `UIGestureRecognizer` stay themselves; unique / edge / gesture-only markers strip; live `data-gesture` plus tap-to-delete copy stays pending without inventing UITapGestureRecognizer or onTapGesture; do not invent a gesture; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Keyboard Don't heuristics account keyboards and virtual-keyboards when every Don't has a scanner and the host has keyboard chrome; gate stays `phone,ipad,desktop`; unknown chrome-pass stays skipped-gate; a phone host with no keyboard chrome is skipped-no-affordance; an input, a text field, and a bare keyboard phrase stay themselves; repurpose / modifier / help / duplicate-key markers strip; live `data-keyboard` plus Command-Z copy stays pending without inventing keyboardShortcut; placeholder-only labels stay on packed forms; Scribble stays on the Pencil pack; do not invent a keyboard; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Pointing Don't heuristics account pointing-devices when every Don't has a scanner and the host has pointer chrome; gate stays `ipad,desktop`; unknown chrome-pass and phone hosts stay skipped-gate; a Mac host with no pointer chrome is skipped-no-affordance; a link `cursor: pointer`, a hover rule, and a bare pointer phrase stay themselves; instruct / decorative markers strip; live `data-pointer` plus instructional-text copy stays pending without inventing UIPointerStyle or NSCursor; system edge swipes stay on gestures; Pencil hover stays on the Pencil pack; do not invent a pointer; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Pencil Don't heuristics account apple-pencil-and-scribble when every Don't has a scanner and the host has pencil chrome; gate stays `ipad+capability:pencil`; unknown chrome-pass, phone, and Mac hosts stay skipped-gate; an iPad host with `import PencilKit` and no pencil canvas is skipped-no-affordance; `import PencilKit`, a bare Apple Pencil phrase, and `data-pe-*` markers alone stay themselves; hover / double-tap / distraction markers strip; live `data-pencil` plus hover-initiates-an-action copy stays pending without inventing PKCanvasView, PKToolPicker, or UIScribbleInteraction; a phone host keeps those markers because the topic stays skipped-gate; pointer rules stay on pointing; do not invent a pencil canvas; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Game controls Don't heuristics account game-controls when every Don't has a scanner and the host has game-control chrome; gate stays `games,capability:games`; unknown chrome-pass, phone, and Mac hosts stay skipped-gate; a game host with no game-control widget is skipped-no-affordance; `import SpriteKit`, `import GameKit`, a Game Center widget, `data-gc-*` markers, and a bare game-controller phrase stay themselves; the letter marker strips; live `data-game-controls` plus A/X/R1 copy, and a live button labeled A, stay pending without inventing GCController or replacement artwork; a phone host keeps the marker because the topic stays skipped-gate; a static thumbstick stays unscanned; fake Game Center overlays stay on game-center; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] iPhone Duo Don't heuristics account designing-for-iphone-duo when every Don't has a scanner and the host has Duo layout chrome; gate stays `duo,capability:duo`; unknown chrome-pass and a current iPhone stay skipped-gate; a Duo host with no `data-duo` layout is skipped-no-affordance; `ArrangementView`, `reservedRegions`, `data-id-*` markers, and a bare iPhone Duo phrase stay themselves; reinvent / fixed / fold markers strip; live `data-duo` plus fixed-width copy, and `UIScreen.main.bounds`, stay pending without inventing ArrangementView or a fold pose; a phone host keeps the markers because the topic stays skipped-gate; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] CareKit Don't heuristics account carekit when every Don't has a scanner and the host has a care-plan widget; dedicated surface `carekit` keeps the Don't off ResearchKit; gate stays `capability:carekit`; unknown chrome-pass stays skipped-gate; a CareKit import with no care-plan widget is skipped-no-affordance; `import CareKit`, `import ResearchKit`, a HealthKit widget, `data-ck-ad`, and a bare CareKit phrase stay themselves; the advertising marker strips; live `data-carekit` plus advertising copy, and a Sponsored line, stay pending without inventing OCKCarePlanStore or a permission sheet; a ResearchKit host keeps the marker because carekit stays skipped-gate; Health permission stays human-only; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] ResearchKit Don't heuristics account researchkit when every Don't has a scanner and the host has a study widget; dedicated surface `researchkit` keeps the Don't off CareKit; gate stays `capability:researchkit`; unknown chrome-pass stays skipped-gate; a ResearchKit import with no study widget is skipped-no-affordance; `import ResearchKit`, `import CareKit`, a care-plan widget, `data-rk-critical`, and a bare ResearchKit phrase stay themselves; the marker strips; live `data-researchkit` plus the critical-data sentence, and a "not critical to the study" line, stay pending without inventing a permission sheet or consent flow; a CareKit host keeps the marker because researchkit stays skipped-gate; Health permission and informed consent stay human-only; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Wallet Don't heuristics account wallet when every Don't has a scanner and the host has a pass widget; dedicated surface `wallet` keeps the Don't off Apple Pay; gate stays `capability:wallet`; unknown chrome-pass stays skipped-gate; a PassKit import with no pass widget is skipped-no-affordance; `import PassKit`, `PKPassLibrary`, an Apple Pay button, `data-wl-marketing`, and a bare Wallet phrase stay themselves; the marker strips; live `data-wallet` plus the change-message sentence, and a `changeMessage` sale, stay pending without inventing an Add to Wallet sheet; an Apple Pay host keeps the marker because wallet stays skipped-gate; decline-and-ask-again stays unscanned; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] App Clip Code Don't heuristics account app-clips when every Don't has a scanner and the host has an App Clip Code widget; dedicated surface `app-clips` keeps the Don't off the platform cluster; gate stays `capability:appclips`; unknown chrome-pass stays skipped-gate; an App Clip entitlement with no code widget is skipped-no-affordance; the entitlement, `data-app-clip`, `data-ac-modified`, an ad sentence, and a bare App Clip phrase stay themselves; the marker strips; live `data-app-clip-code` plus the homemade-code sentence, and a `drop-shadow` on the code, stay pending without inventing an App Clip Code; a host with no App Clip capability keeps the marker because app-clips stays skipped-gate; ads, web views, and account walls stay unscanned; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] App Clip Code presentation Don't heuristics account overlay, motion, and rotation when every Don't has a scanner and the host has an App Clip Code widget; same surface `app-clips`; gate stays `capability:appclips`; `data-ac-overlay`, `data-ac-motion`, and `data-ac-rotate` are not the widget; those markers strip; a logo image, an animation, and a rotation on the code stay pending without inventing an App Clip Code; a host without the capability keeps the markers; ads stay unscanned; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] App Clip Code aspect-ratio Don't heuristics account a changed aspect ratio when every Don't has a scanner and the host has an App Clip Code widget; same surface `app-clips`; gate stays `capability:appclips`; `data-ac-aspect` is not the widget; the marker strips; `object-fit: fill` and a non-uniform `scale()` on the code stay pending without inventing an App Clip Code; letterbox stays on playing-video; a stretched photo stays on images; a host without the capability keeps the marker; ads stay unscanned; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] App Clip Code symbol Don't heuristics account a symbol added to the code when every Don't has a scanner and the host has an App Clip Code widget; same surface `app-clips`; gate stays `capability:appclips`; `data-ac-symbol` is not the widget; the marker strips; a trademark glyph on the code stays pending without inventing an App Clip Code; an image overlay stays on the overlay scanner; an SF Symbol stays on icons; a host without the capability keeps the marker; ads stay unscanned; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Wallet decline Don't heuristics account a second suggestion after people decline when every Don't has a scanner and the host has a pass widget; same surface `wallet`; gate stays `capability:wallet`; `data-wl-decline` is not the widget; the marker strips; the Apple sentence and a second `Add again` after `declined` stay pending without completing an Add to Wallet sheet; the first Add to Wallet suggestion stays allowed; a camera "don't ask them again" sentence stays itself; Apple Pay stays capability-gated; a host without the capability keeps the marker; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Segment-count Don't heuristics account a segmented control with eight or more segments when every Don't has a scanner; seven or fewer stay already-compliant; `data-sg-count` strips without removing a segment; design-principles stay pending; remaining is not a frozen integer
- [x] Spinner-label Don't heuristics account a label on a spinning progress indicator when every Don't has a scanner; an `aria-label` and a determinate percent stay already-compliant; `data-pg-label` strips without removing the spinner; design-principles stay pending; remaining is not a frozen integer
- [x] Submenu-available Don't heuristics account a submenu item that is unavailable when its nested items are unavailable; a disabled command that does not open a submenu stays already-compliant; `data-mn-sub` strips without enabling the submenu; design-principles stay pending; remaining is not a frozen integer
- [x] Minimal-scrub Don't heuristics account a scrubber on a page control that uses the minimal background style when every Don't has a scanner; minimal without a scrubber stays already-compliant; `data-pgc-scrub` strips without removing the dots; design-principles stay pending; remaining is not a frozen integer
- [x] We-copy Don't heuristics account visible copy that says we or we're when every Don't has a scanner; copy without we stays already-compliant; Welcome stays already-compliant; a comment stays already-compliant; `data-wr-we` strips without rewriting the sentence; design-principles stay pending; remaining is not a frozen integer
- [x] Vague-progress Don't heuristics account a determinate progress indicator labeled Loading or Authenticating when every Don't has a scanner; a specific description stays already-compliant; a longer phrase stays already-compliant; an `aria-label` stays already-compliant; `data-pg-vague` strips without rewriting the label; design-principles stay pending; remaining is not a frozen integer
- [x] Duplicate-notification Don't heuristics account multiple notifications for the same thing when every Don't has a scanner; two different titles stay already-compliant; one notification stays already-compliant; `data-nt-dup` strips without removing a notification; design-principles stay pending; remaining is not a frozen integer
- [x] App Clip logo Don't heuristics account the App Clip logo used on its own when every Don't has a scanner; a generated code stays already-compliant; the logo drawn with the generated code stays already-compliant; ads stay already-compliant; `data-ac-solo` strips without removing the logo; design-principles stay pending; remaining is not a frozen integer
- [x] Drag-morph Don't heuristics account a drag image that is constantly and radically changing when every Don't has a scanner; a stable preview stays already-compliant; one preview image stays already-compliant; `data-dg-morph` strips without deleting the preview; design-principles stay pending; remaining is not a frozen integer
- [x] Care-symbol Don't heuristics account a corporate logo or purely decorative symbol used as the care symbol when every Don't has a scanner; a heart symbol stays already-compliant; advertising stays on its own scanner; `data-ck-logo` strips without removing the symbol; design-principles stay pending; remaining is not a frozen integer
- [x] Photo-toolbar Don't heuristics account a custom top toolbar in a photo-editing session when every Don't has a scanner; a session with no second toolbar stays already-compliant; a file-browser toolbar stays on file-management; `data-px-toolbar` strips without removing the system toolbar; design-principles stay pending; remaining is not a frozen integer
- [x] Wallet strip Don't heuristics account text embedded in the strip image when every Don't has a scanner; a strip graphic without text stays already-compliant; text drawn over the strip stays already-compliant; padding on a logo stays already-compliant; `data-wl-strip` strips without rewriting the image; design-principles stay pending; remaining is not a frozen integer
- [x] Plain-text password Don't heuristics account a password written to a plain-text file when every Don't has a scanner; a password field stays already-compliant; a keychain write stays already-compliant; a text file that does not store a password stays already-compliant; the Apple sentence alone stays already-compliant; `data-pv-plain` strips without removing the file write; design-principles stay pending; remaining is not a frozen integer
- [x] Fake-badge Don't heuristics account a custom component that mimics a notification badge when every Don't has a scanner; the system `applicationIconBadgeNumber` stays already-compliant; a plain badge chip stays already-compliant; `data-nt-mimic` strips without removing the badge; design-principles stay pending; remaining is not a frozen integer
- [x] Alert-title-lines Don't heuristics account an alert title that wraps to more than two lines when every Don't has a scanner; a one-line or two-line title stays already-compliant; a long message under a short title stays already-compliant; a sheet title stays already-compliant; `data-al-lines` strips without shortening the title; design-principles stay pending; remaining is not a frozen integer
- [x] Toolbar-unnamed Don't heuristics account a toolbar item with no visible text and no accessible name when every Don't has a scanner; a symbol with an accessible name stays already-compliant; a text button stays already-compliant; an unnamed button outside a toolbar stays already-compliant; `data-tb-name` strips without inventing a label; design-principles stay pending; remaining is not a frozen integer
- [x] Window-app-title Don't heuristics account a window titled with the app name when every Don't has a scanner; a content title stays already-compliant; a document title stays already-compliant; `UIWindow` stays already-compliant; `data-wn-app` strips without rewriting the title; design-principles stay pending; remaining is not a frozen integer
- [x] Sidebar-depth Don't heuristics account a third level of hierarchy in a sidebar when every Don't has a scanner; two levels stay already-compliant; a nested list outside a sidebar stays already-compliant; sections in the detail column stay already-compliant; `data-sb-depth` strips without flattening the list; design-principles stay pending; remaining is not a frozen integer
- [x] Column-punctuation Don't heuristics account a column heading that ends with punctuation when every Don't has a scanner; a heading without punctuation stays already-compliant; a single-column table stays already-compliant; an outline colon stays on the outline scanner; `data-hd-punct` strips without rewriting the heading; design-principles stay pending; remaining is not a frozen integer
- [x] Context-shortcut Don't heuristics account a keyboard shortcut inside a context menu when every Don't has a scanner; a shortcut on a main menu stays already-compliant; a shortcut on a button stays already-compliant; the Apple sentence without a context menu stays skipped; `data-mn-key` strips without removing the shortcut; design-principles stay pending; remaining is not a frozen integer
- [x] Color-only toggle Don't heuristics account a toggle that relies solely on different colors to communicate state when every Don't has a scanner; an icon, a checkmark, or a labeled switch stays already-compliant; the Apple sentence without that control stays already-compliant; `data-tg-color` strips without recoloring the control; design-principles stay pending; remaining is not a frozen integer
- [x] Selection-label Don't heuristics account a label that explains a button that changes the selection when every Don't has a scanner; an image-only selection button stays already-compliant; a labeled switch stays already-compliant; `data-tg-sel` strips without removing the label; design-principles stay pending; remaining is not a frozen integer
- [x] Scrub-animation Don't heuristics account an animated page transition while a page control is scrubbing when every Don't has a scanner; a scrubber without that animation stays already-compliant; `data-pgc-anim` strips without removing the dots; design-principles stay pending; remaining is not a frozen integer
- [x] Scroll-indicator Don't heuristics account a scrolling indicator on a scroll view that also shows a page control when every Don't has a scanner; a scroll view without a page control stays already-compliant; a hidden indicator stays already-compliant; `data-sv-indicator` strips without hiding the indicator; design-principles stay pending; remaining is not a frozen integer
- [x] Segment-role Don't heuristics account a segmented control that both selects and acts when every Don't has a scanner; a radio-only control stays already-compliant; `data-sg-role` strips without restyling a segment; design-principles stay pending; remaining is not a frozen integer
- [x] Index-beside-disclosure Don't heuristics account a section index on a list that also shows disclosure indicators when every Don't has a scanner; either widget alone stays already-compliant; the Apple sentence without a list stays already-compliant; `data-ix-both` strips without removing the index; design-principles stay pending; remaining is not a frozen integer
- [x] Segment-mix Don't heuristics account a segmented control that mixes text and images when every Don't has a scanner; an image-only control, a text-only control, and segments that each carry both a word and an image stay already-compliant; an `aria-label` is not visible text; a text segment beside an image-only segment stays pending; `data-sg-mix` strips without restyling a segment; design-principles stay pending; remaining is not a frozen integer
- [x] Too-many-radios Don't heuristics account a set of more than about five radio buttons when every Don't has a scanner; a set of five or fewer stays already-compliant; a `role="radio"` view switcher stays already-compliant; six radios that share a name stay pending; a SwiftUI radio-group picker with more than five tags stays pending; the Apple sentence without a radio stays already-compliant; `data-tg-radios` strips without removing the radio; design-principles stay pending; remaining is not a frozen integer
- [x] Tab-bar disabled Don't heuristics account a disabled or hidden tab bar button when every Don't has a scanner; a disabled button that is not a tab stays already-compliant; a tab view with a tab panel stays already-compliant; `data-tb-off` strips without enabling the tab; design-principles stay pending; remaining is not a frozen integer
- [x] Sheet Done-only Don't heuristics account relying solely on the Done button to exit a sheet when every Don't has a scanner; a lone Done button in a dialog stays already-compliant; a `.sheet` whose only button is Done stays pending; Done paired with Cancel, Close, or Back stays already-compliant; the three-button trio stays on its own scanner; a Done action on an alert controller stays already-compliant; a Done button outside a sheet stays skipped; `data-sh-done` strips without inventing Cancel; design-principles stay pending; remaining is not a frozen integer
- [x] Sheet trio Don't heuristics account Cancel, Done, and Back together in a sheet when every Don't has a scanner; a Done and Cancel pair and a Done and Back pair stay already-compliant; the three titles on an alert controller stay already-compliant; the three buttons outside a sheet stay skipped; `data-sh-trio` strips without removing a button; design-principles stay pending; remaining is not a frozen integer
- [x] Alert caution-symbol Don't heuristics account a caution symbol on a Save or Empty Trash alert when every Don't has a scanner; a Delete button with the symbol and a Save button without it stay already-compliant; the symbol outside an alert stays skipped; `UIAlertAction` title Save stays pending without removing the symbol; `data-al-caution` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Alert Yes/No Don't heuristics account a Yes or No button in an alert when every Don't has a scanner; a Delete button and a No thanks button stay already-compliant; a Yes button outside an alert stays skipped; `UIAlertAction` title Yes stays pending without rewriting the label; `data-al-yes` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Alert cancel-default Don't heuristics account a Cancel button that is the default button in an alert when every Don't has a scanner; a plain Cancel button and a primary Done button stay already-compliant; a Cancel button outside an alert stays skipped; `UIAlertAction` style `.cancel` stays already-compliant; style `.default` on Cancel stays pending without rewriting the label; `data-al-cancel` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Alert error-title Don't heuristics account an alert title that is only Error or an error number when every Don't has a scanner; a specific title stays already-compliant; a status toast that says Error stays skipped; SwiftUI `.alert(` alone does not launch the surface; `UIAlertController` title Error stays pending without rewriting the title; `data-al-error` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Primary destructive Don't heuristics account the primary role on a destructive button when every Don't has a scanner; a plain Delete button, a destructive role without a prominent style, and a prominent Save stay already-compliant; a primary, submit, or prominent Delete stays pending without restyling the button; `data-bt-primary` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Tracking leave Don't heuristics account a Close or Cancel button on a custom tracking screen when every Don't has a scanner; Continue and Next stay already-compliant; a legal-consent Cancel stays already-compliant; an Allow button stays on the pre-alert scanner; a camera Cancel stays already-compliant; the Apple sentence stays pending only when a tracking request is present; `data-pv-leave` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Tracking pre-alert Don't heuristics account a custom tracking screen with an incentive or an Allow button when every Don't has a scanner; Continue before `requestTrackingAuthorization` stays already-compliant; an Allow button and an earn-coins line stay pending without inventing a screen; a camera Allow button stays already-compliant; the Apple sentence stays pending only when a tracking request is present; `data-pv-att` strips; design-principles stay pending; remaining is not a frozen integer
- [x] Notification content-name Don't heuristics account the app name or icon inside the notification content when every Don't has a scanner and the host sends notifications; a content title that repeats `CFBundleDisplayName` stays pending; New message, a title with no display name, and an AppIcon attachment stay already-compliant; an action title stays on the label scanner; `data-nt-content` strips when a notification center is present; the Apple sentence stays pending without inventing a content title; a marker with no notification center stays; design-principles stay pending; remaining is not a frozen integer
- [x] Notification open-action Don't heuristics account an action that merely opens your app when every Don't has a scanner and the host sends notifications; a title that is exactly Open stays pending; a Snooze title stays already-compliant; Open Acme without a display name stays already-compliant; Open Acme with `CFBundleDisplayName` stays pending on the label scanner; `data-nt-open` strips when a notification center is present; the Apple sentence stays pending without inventing an action; a marker with no notification center stays; design-principles stay pending; remaining is not a frozen integer
- [x] Notification action-label Don't heuristics account the app name in a notification button label when every Don't has a scanner and the host sends notifications; `CFBundleDisplayName` must already be written; a Snooze title, a title with no display name, and a two-letter name stay already-compliant; `data-nt-label` strips when a notification center is present; the Apple sentence and an Open title that repeats the display name stay pending without inventing an action; a marker with no notification center stays; design-principles stay pending; remaining is not a frozen integer
- [x] Notification badge Don't heuristics account a badge that conveys numeric information that isn't related to notifications when every Don't has a scanner and the host sends notifications; `applicationIconBadgeNumber` is not the widget; `data-nt-badge` is not the widget; the marker strips when a notification center is present; the Apple sentence and a weather value on the badge stay pending; an unread count and an action-label sentence stay already-compliant; a badge with no notification center keeps the marker; design-principles stay pending; remaining is not a frozen integer
- [x] Wallet logo Don't heuristics account inner drop shadows on logo artwork when every Don't has a scanner and the host has a pass; same surface `wallet` and `data-wallet`; gate stays `capability:wallet`; `data-wl-shadow` is not the widget; the marker strips; the Apple sentence and an inset shadow on the logo tag stay pending without inventing a logo; an inset shadow on the pass chrome, padding on the logo, and strip-image text stay already-compliant; Apple Pay does not launch the surface; a host without the capability keeps the marker; do not overlay `always`; design-principles stay pending; remaining is not a frozen integer
- [x] Photo-editing Cancel Don't heuristics account Cancel that discards edits without a confirm when every Don't has a scanner and the host has an editing session; dedicated surface `photo-editing` sits before the media cluster; gate stays `capability:photos`; `import PhotosUI` is not the widget; `data-px-cancel` is not the widget; the marker strips; the Apple sentence and Cancel that discards existing edits stay pending without inventing a confirm dialog; no edits yet, a confirm that says edits will be lost, and a custom top toolbar sentence stay already-compliant; `import ShazamKit` does not launch the surface; a host without the capability keeps the marker; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] ShazamKit microphone Don't heuristics account a microphone that stays on after recognition when every Don't has a scanner and the host has a recognition session; dedicated surface `shazamkit` sits before the media cluster; gate stays `capability:shazam`; `import ShazamKit` is not the widget; `data-sz-mic` is not the widget; the marker strips; the Apple sentence and `.match()` plus `.start()` without `.stop()` stay pending without completing a microphone permission sheet; a session that stops and an iCloud song-library opt-in stay already-compliant; `import CoreML` does not launch the surface; a host without the capability keeps the marker; do not overlay `always`; the Mac menu bar stays an unpackaged stub; design-principles stay pending; remaining is not a frozen integer
- [x] Same chrome P0 FAIL on a web host and a Swift host; grammar `failWhen` / `passWhen` do not name frameworks
- [x] Packed catalog topics derive `failWhen` from grammar + pack Do/Don't at load; unpackaged rows stay stubs; no SF Pro / framework names
- [x] Mechanical apply clears the two dual-stack P0s on tmp copies of the web and Swift antipattern hosts; chrome-pass stays clean; no SF Pro/kit; committed fixtures stay dirty
- [x] Mechanical apply clears remaining P0 IDs (and nested-cards) on a tmp copy of `chrome-antipatterns`; filter-density and sidebar may remain; committed fixtures stay dirty
- [x] Mechanical apply clears filter-density and sidebar on a tmp copy of `chrome-antipatterns`; after.fails is empty; committed fixtures stay dirty
- [x] Catalog apply accounts chrome-backed packed topics on tmp chrome-pass (already-compliant) and tmp chrome-antipatterns (applied after chrome fix); pack-only rows stay pending; remaining is not a frozen integer
- [x] Pack Chrome gates join catalog chromeIds (buttons/text-fields); pack Don't code spans apply on a tmp host (`scaleX(-1)` / `margin-left`); the-menu-bar stays a title stub; no SF Pro/kit
- [x] Required-surface prose Don'ts (type/color/motion/a11y) scan/apply/account on tmp chrome-pass and tmp antipattern files; a topic accounts only when every Don't has a scanner; missing optional widgets are skipped-no-affordance; no SF Pro/kit; remaining is not a frozen integer

## Swarm / stack (0.4)

- [x] SwiftUI fixture: mutation open
- [x] Web CSS fixture (no React): mutation open
- [x] Docs-only fixture still unsupported
- [x] Optional hig-react exists and does not inject a kit
- [x] Asset-catalog flood still finds `.swift` (`kind=swiftui`)
- [x] Help HTML next to SwiftUI stays `native-apple`, not `mixed`
- [x] LaunchScreen.storyboard + leftover `.xib` do not force UIKit
