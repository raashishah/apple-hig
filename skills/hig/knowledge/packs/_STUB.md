# Pack stub

This section is registered in `knowledge/registry.yaml` but the full Apple-doc distillation is not authored yet.

When crafting:

1. Open the section's `hig_url` from the registry
2. Apply `/hig` shared laws + brand from DESIGN.md
3. Prefer adjacent authored packs (foundations-*) for materials/layout/type
4. After implementing, consider promoting notes into a real pack file
